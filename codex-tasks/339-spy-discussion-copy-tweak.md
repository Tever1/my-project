# TASK-339: Шпион — правка текста на экране «Обсуждение»

> **Метаданные** (заполняет Claude перед стартом)
> - **Дата создания:** 2026-07-12
> - **Сложность:** simple
> - **Запуск:** auto by Claude
> - **Ожидаемое время Codex:** ~2 минуты
> - **Зависит от тасков:** TASK-338

---

## Цель

На экране фазы «Обсуждение» (мобильный клиент Шпиона) убрать слово
«вслух» и убрать фразу про автоматическое начало голосования (это и так
видно по таймеру).

---

## Контекст

Live-QA фидбек сразу после TASK-338. Одна строка текста.

---

## Файлы к изменению (whitelist)

- `src/app/game/[roomId]/spy/page.tsx`

### НЕ ТРОГАТЬ

- Ничего другого — только эта одна строка текста.
- `CLAUDE.md`, `AGENTS.md` — обновляет только Claude.

---

## Шаги реализации

В `src/app/game/[roomId]/spy/page.tsx`, строка ~1407, найти:

```tsx
{l(
  'Обсудите вслух, кто вам кажется подозрительным — голосование начнётся автоматически.',
  'Discuss out loud who seems suspicious — voting will start automatically.',
)}
```

Заменить на:

```tsx
{l(
  'Обсудите, кто вам кажется подозрительным.',
  'Discuss who seems suspicious.',
)}
```

---

## Acceptance criteria

- [ ] `npm run lint` без новых ошибок
- [ ] `npx tsc --noEmit` без новых ошибок
- [ ] Текст на экране обсуждения — «Обсудите, кто вам кажется
      подозрительным.» (без «вслух» и без фразы про автоматическое
      голосование)

---

## Контрольные точки для самопроверки Codex

1. Прочитать diff.
2. Убедиться что изменена только эта одна строка в одном файле.
3. Запустить `npm run lint` и `npx tsc --noEmit`.
4. Заполнить отчёт `codex-reports/339-spy-discussion-copy-tweak.md`.
5. **Не коммитить.**

---

## Открытые вопросы для Codex

Нет.
