# TASK-129.1: Quiz answer button radius → 6px (rounded-md)

> **Метаданные**
> - **Дата создания:** 2026-05-22
> - **Сложность:** simple
> - **Запуск:** auto by Claude
> - **Ожидаемое время Codex:** ~2 минуты

## Цель

В `src/app/game/[roomId]/quiz/page.tsx` заменить `rounded` → `rounded-md` на кнопке ответа (`motion.button` с `variants={answerVariants}`).

## Файлы к изменению (whitelist)

- `src/app/game/[roomId]/quiz/page.tsx` — одна замена

### НЕ ТРОГАТЬ

- все остальные файлы

## Шаги

Найти строку:
```
className={`relative overflow-hidden rounded border p-5 ...`}
```
Заменить `rounded` → `rounded-md`.

## Acceptance criteria

- [ ] `npm run lint` без ошибок
- [ ] В кнопке ответа квиза: `rounded-md` (не `rounded`, не `rounded-xl`)
- [ ] Не коммитить, отчёт в `codex-reports/129.1-quiz-answer-radius-6px.md`
