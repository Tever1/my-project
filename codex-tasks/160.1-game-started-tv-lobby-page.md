# TASK-160.1 — Подключить `useNavigateOnGameStart` в TV-лобби страницу

## Контекст

TASK-160 подключил хук `useNavigateOnGameStart` в join/[code] и Lobby,
но пропустил третий файл — `src/app/tv/[roomId]/page.tsx` (TV-лобби
до старта игры). Там тот же паттерн: `on('game:started', ...)` → `/tv/...`.

## Whitelist

**Изменить:**
- `src/app/tv/[roomId]/page.tsx`

**Создать:**
- `codex-reports/160.1-game-started-tv-lobby-page.md`

**НЕЛЬЗЯ трогать:** всё остальное.

## Что сделать

В `src/app/tv/[roomId]/page.tsx` текущий `useEffect` (~строки 42-56):
```ts
useEffect(() => {
  const unsub = on('room:state', (data: unknown) => {
    setRoom(data as RoomState);
  });

  const unsubStarted = on('game:started', (data: unknown) => {
    const { gameType, roomCode } = data as { gameType: string; roomCode: string };
    router.push(`/tv/${roomCode}/${gameType}`);
  });

  return () => {
    unsub();
    unsubStarted();
  };
}, [roomId, on, router]);
```

**Разделить на два:**

1. Оставить `room:state` listener как отдельный `useEffect`:
```ts
useEffect(() => {
  return on('room:state', (data: unknown) => {
    setRoom(data as RoomState);
  });
}, [on]);
```

2. Добавить вызов хука вместо `game:started` listener'а:
```ts
useNavigateOnGameStart(
  ({ roomCode, gameType }) => `/tv/${roomCode}/${gameType}`,
);
```

Добавить импорт:
```ts
import { useNavigateOnGameStart } from '@/lib/use-navigate-on-game-start';
```

Если после изменений `router` больше нигде в файле не используется —
удали `const router = useRouter()` и импорт `useRouter`.

## Acceptance

```bash
grep -rn "game:started" src/app/ src/components/
# → 0 совпадений
npm run lint     # ✅
npx tsc --noEmit # ✅
```

Не коммить, не пушить.
