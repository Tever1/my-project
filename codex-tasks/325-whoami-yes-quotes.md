# TASK-325: «Кто я?» — кавычки вместо скобок в бейдже серии «Да»

## Контекст

TASK-324 поставила слово «Да» в круглые скобки: `(Да) подряд: N/3`.
Продакт уточнил — нужны были КАВЫЧКИ, не скобки: `«Да» подряд: N/3`.

## Файл

`src/app/game/[roomId]/who-am-i/page.tsx`, бейдж рядом с «Вопросов
задано» (искать `(Да) подряд`).

## Что сделать

Заменить русский текст `(Да) подряд: ${gs.consecutiveYesAnswers}/3` на
`«Да» подряд: ${gs.consecutiveYesAnswers}/3` (ёлочки, не программистские
двойные кавычки). Английскую версию `Yes streak: N/3` не трогать.

## Whitelist файлов

- `src/app/game/[roomId]/who-am-i/page.tsx`

## Acceptance

- `npm run lint` и `tsc --noEmit` без новых ошибок.
- Русский текст бейджа — `«Да» подряд: N/3`.

Не коммить. Отчёт в `codex-reports/325-whoami-yes-quotes.md`.
