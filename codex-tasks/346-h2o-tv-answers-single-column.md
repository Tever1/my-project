# TASK-346: «100 к 1» — игровое поле (TV): ответы в один столбец

**Тип:** simple (1 файл, изменение layout-класса)
**Whitelist:** `src/app/tv/[roomId]/[gameType]/page.tsx`
**НЕ трогать:** любой другой блок TV (другие игры), мобильный экран, server.mts.

---

## Контекст

Пользователь начинает серию задач по редизайну игры «100 к 1» — эта первая.
«Игровое поле» = TV-экран (терминология проекта, см. CLAUDE.md).

## Задача

В блоке `gameType === 'hundred-to-one'`, фаза `playing` (~строка 1233),
сетка ответов:

```tsx
<div className="grid grid-cols-2 gap-3 w-full mb-4">
```

Поменять на один столбец:

```tsx
<div className="grid grid-cols-1 gap-3 w-full mb-4">
```

**Только этот грид** (основной экран раунда 1-3/4, где отвечает `q.answers`).
Не трогай сетку в фазе Round 4 rules (~строка 2184, `grid-cols-2` там —
другой контекст, экран правил, не ответы текущего вопроса) — если не уверен,
какой именно грид имеется в виду, спроси в отчёте вместо изменения нескольких
мест.

## Acceptance

- `npm run lint` и `npx tsc --noEmit` чисто.
- Единственное изменение — `grid-cols-2` → `grid-cols-1` в указанном месте.
- НЕ коммитить. Отчёт в `codex-reports/346-h2o-tv-answers-single-column.md`.
