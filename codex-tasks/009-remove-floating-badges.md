# TASK-009: Удалить floating-бейджи «8 онлайн» и «2 480 лучший рекорд»

> **Метаданные**
> - **Дата создания:** 2026-05-03
> - **Сложность:** simple
> - **Запуск:** auto by Claude
> - **Ожидаемое время Codex:** ~2 минуты
> - **Зависит от тасков:** —

---

## Цель

Полностью удалить из `src/app/lobby-preview/page.tsx` два floating-бейджа,
которые накладываются на TiltedPreview-карточку и закрывают её контент:
- «8 онлайн / сейчас в игре» (top-right)
- «2 480 / лучший рекорд» (bottom-left)

Также удалить сам компонент `FloatingBadge` и все связанные с ним
импорты/типы (если он больше нигде не используется).

---

## Контекст

Бейджи decoративные, на узких десктопах (1025-1300) перекрывают цифры
ответов и таймер в карточке предпросмотра игры. Пользователь решил
убрать их совсем.

`grep -n "FloatingBadge"` показывает 3 точки в файле:
- строка ~761: первый `<FloatingBadge .../>` (8 онлайн)
- строка ~769: второй `<FloatingBadge .../>` (2 480 рекорд)
- строка ~1084: определение `function FloatingBadge(...)`

---

## Файлы к изменению (whitelist)

- `src/app/lobby-preview/page.tsx` — удалить 2 jsx-блока + объявление
  `function FloatingBadge`.

### НЕ ТРОГАТЬ

- Никакие другие файлы.
- Сам компонент `TiltedPreview` — оставить как есть (только удалить
  внутри него 2 `<FloatingBadge ...>` вызова).

---

## Шаги реализации

1. В `TiltedPreview` (строки ~760-776) удалить оба jsx-блока
   `<FloatingBadge ... />`. Удалить пустую строку между бейджами и
   закрытием `</div>` если она остаётся.
2. Удалить определение `function FloatingBadge` (~строки 1084-конец
   функции, до следующей `function`-декларации).
3. Если в файле после удаления остались неиспользуемые импорты/типы
   связанные ТОЛЬКО с FloatingBadge — удалить их. Если они используются
   и в других местах (например `motion.div` — точно используется в
   других компонентах) — НЕ ТРОГАТЬ.
4. Проверить что после удаления в файле нет ссылок на `FloatingBadge`
   (`grep -n FloatingBadge` должен ничего не находить).

---

## Acceptance criteria

- [ ] `npm run lint` без новых ошибок.
- [ ] `npm run build` успешен.
- [ ] `grep -rn FloatingBadge src/` ничего не находит.
- [ ] Визуально TiltedPreview карточка остаётся, но без всплывающих
      бейджей вокруг неё (это проверит Claude через preview).
- [ ] Никакие другие изменения не внесены.

---

## Ограничения и подводные камни

- **Не удалять `motion.div`** — он используется в других компонентах
  страницы.
- **Не трогать sample mock-content** в TiltedPreview (PreviewQuestion,
  PreviewAnswers, PreviewTimer и т.д.) — только бейджи.
- **Не переписывать оставшийся код** «попутно».

---

## Контрольные точки для самопроверки Codex

1. `git diff --stat` — должен быть только `src/app/lobby-preview/page.tsx`.
2. `grep -n FloatingBadge src/app/lobby-preview/page.tsx` — пусто.
3. `npm run lint` без новых ошибок.
4. Заполнить `codex-reports/009-remove-floating-badges.md`.
5. Не коммитить.
