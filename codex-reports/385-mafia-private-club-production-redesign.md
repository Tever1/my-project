# REPORT TASK-385: Интеграция дизайна «Закрытый клуб» в Мафию

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-05 19:45 PDT
> - **Финиш:** 2026-08-05 21:14 PDT
> - **Длительность:** 89 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Одобренное направление «Закрытый клуб» перенесено из preview в настоящую игру
Мафия: полностью обновлены мобильные экраны и TV-сцены всех фаз. Игровая логика,
socket-события и состав ролей сохранены; роль на телефоне раскрывается только во
время удерживания и снова скрывается после отпускания.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/mafia/page.tsx` — новые production-представления лобби,
  раскрытия роли, ночных действий всех ролей, дня, трёх раундов голосования,
  вердикта и финала; добавлено настоящее удерживание карточки.
- `src/app/tv/[roomId]/[gameType]/page.tsx` — полноценные TV-сцены в стиле клуба,
  визуальный прогресс голосования, итоги ночи, исключённые игроки и финальное
  раскрытие ролей; TV-state дополнен необходимыми данными из существующих событий.

### Новые файлы

- `src/components/games/mafia-club/MafiaClub.tsx` — общие production-компоненты
  телефона и TV, карточки/миниатюры существующих изображений ролей, рамка и токены
  игроков.
- `src/components/games/mafia-club/MafiaClub.module.css` — визуальная система
  «Закрытого клуба»: тёмный фиолетовый бархат, латунь, винный danger-state,
  адаптивность, safe areas и reduced motion.

### Удалённые файлы

- (нет)

---

## Diff stat

```text
 src/app/game/[roomId]/mafia/page.tsx    | 421 +++++++++++++++++++++-----------
 src/app/tv/[roomId]/[gameType]/page.tsx | 339 ++++++++++++++++++-------
 2 files changed, 528 insertions(+), 232 deletions(-)

 Новые общие компоненты: 462 строки в двух файлах.
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint -- --quiet` | ✅ | Ошибок нет |
| `npx tsc --noEmit` | ✅ | Ошибок типов нет |
| `npm run build` | ✅ | Exit code 0; существующее нефатальное сообщение `location is not defined` осталось |
| `git diff --check` | ✅ | Ошибок пробелов нет |
| HTTP smoke test | ✅ | `/mafia-design-preview`, `/game/TEST/mafia`, `/tv/TEST/mafia` отвечают 200 |
| Все мобильные фазы | ✅ | Сохранены действия mafia/don/detective/doctor/maniac/lover/citizen, host и observer |
| Все TV-фазы | ✅ | Lobby, reveal, night, day, voting/revote/verdict, elimination и winner |
| Скрытие роли | ✅ | Карточка видна только при pointer/keyboard hold; после отпускания роль и миниатюра не остаются на экране |
| Accessibility | ✅ | Focus-visible, семантические кнопки, touch targets, safe areas, reduced motion |

---

## Отклонения от ТЗ

Встроенное управление браузером заблокировало доступ к `localhost` своей
политикой безопасности, поэтому повторный визуальный QA production-роутов через
него выполнить нельзя. Визуальная концепция ранее проверена и утверждена на
preview в TASK-384; production-интеграция дополнительно проверена сборкой,
типами, SSR/HTTP-ответами и анализом адаптивной вёрстки.

---

## Открытые вопросы для Claude

Нет.

---

## Подсказки для ревью

- Особо проверить `src/app/game/[roomId]/mafia/page.tsx`: новая UI-ветка должна
  оставаться только отображением существующего host-authoritative состояния.
- В `src/app/tv/[roomId]/[gameType]/page.tsx` TV теперь накапливает только
  презентационные данные (`votesReceived`, последние исключённые/убитые), не
  принимает игровых решений.
- TASK-382/383/384 и изменения изображений ролей уже находились в незакоммиченном
  worktree до TASK-385; они сохранены без отката.
