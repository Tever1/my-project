# TASK-520 — Alias: итоги, команды, счётчики и смена буквы

## Выполнено

- Исправлена воспроизведённая потеря истории слов: приватный snapshot хоста скрывал turnHistory, а следующий alias:state затирал серверную историю. Сервер теперь сохраняет ledger текущего хода и добавляет только новые записи по приросту счётчиков; переход к следующему ходу очищает историю. Публичные итоги возвращаются также отправителю-хосту. Во время объяснения история по-прежнему скрыта от TV и остальных телефонов.
- После подтверждения обоих названий Classic остаётся в teamName: оба названия показаны на phone/TV, продолжение — только кнопкой хоста. Кнопка и callback недоступны до обоих подтверждений.
- На телефоне объясняющего над карточкой слева от «ИДЁТ ХОД» добавлены компактные счётчики угаданных и пропущенных.
- В Letter при пропуске выбирается другая буква, как при угадывании. Подсказки RU/EN обновлены на phone/TV; штраф за пропуск не добавлен.

## Scope

`src/app/game/[roomId]/alias/page.tsx`, Alias-ветка `src/app/tv/[roomId]/[gameType]/page.tsx`, пять scoped CSS-правил в `src/app/globals.css`, Alias sync в `src/server/game-security.mts` и `src/server/socket-handlers.mts`, новый `src/server/alias-history.test.mts`.

Существующие dirty-изменения других игр и предыдущих задач сохранены. Общие канонические документы не редактировались. Номер выбран по фактическому максимуму519; канонический next519 отстаёт, системному чату предстоит синхронизация.

## Проверки

- Диагностика через минимальный regression test: до исправления оба режима теряли все записи кроме последней; после исправления оба проходят. Проверены guessed/skipped списки, Classic final-word, Letter timer expiry, отсутствие повторных записей, сброс следующего хода и скрытая история хоста.
- `node --import tsx --test src/server/alias-history.test.mts src/lib/alias-classic.test.mts src/server/game-security.test.mts`: 27/27.
- `npx tsc --noEmit`, scoped ESLint, `git diff --check`: успешно.
- Phone/TV композиции просмотрены по исходникам; browser/multiplayer QA и реальные размеры не проверены. Регрессионный тест воспроизводит обмен приватными snapshot/reducer, не полноценный браузерный сценарий.

## Осталось

Серверная часть требует разрешённого перезапуска (он уничтожит текущие комнаты). Затем нужен пользовательский phone/TV QA: хост объясняет и хост наблюдает, итоговые списки, названия/подтверждение, счётчики и смена буквы при пропуске. Перед публикацией требуется отдельный свежий review server/shared TV. Build, restart, commit, push не выполнялись. Субагенты и замеры лимитов не использовались.
