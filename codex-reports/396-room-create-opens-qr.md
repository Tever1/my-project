# REPORT TASK-396: Автоматически открывать QR после создания комнаты

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-09
> - **Финиш:** 2026-08-09
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Новая комната теперь создаётся на сервере сразу с активным состоянием QR.
Это устраняет гонку между первоначальным снимком `showQrCode: false` и
последующим клиентским событием открытия QR.

---

## Что сделано

### Изменённые файлы

- `src/server/socket-handlers.mts` — начальное значение `showQrCode` новой
  комнаты изменено с `false` на `true`.
- `codex-reports/CODEX-HANDOFF.md` — добавлена TASK-396 и следующий свободный
  номер TASK-397.

### Новые файлы

- `codex-reports/396-room-create-opens-qr.md` — этот отчёт.

### Удалённые файлы

- Нет.

---

## Diff stat

```text
 src/server/socket-handlers.mts | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ✅ | Ветка актуальна |
| `npx tsc --noEmit` | ✅ | Ошибок нет |
| `npm run lint` | ✅ | 0 ошибок; 4 прежних warning в Mafia page |
| `git diff --check` | ✅ | Ошибок нет |
| Перезапуск dev-сервера | ✅ | Сервер работает на `localhost:3000` |
| Socket.io synthetic QA | ⚠️ | Два запуска не подключились из отдельного CLI-процесса (`websocket error`); состояние сервера не изменили |
| Ручной UI QA | ⏳ | Создать новую комнату кнопкой «Начать партию» и проверить немедленный QR |

---

## Отклонения от ТЗ

Нет.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано

- Автоматическая Socket.io-проверка не смогла подключиться из отдельного
  процесса текущего окружения; финальное подтверждение оставлено живому UI.

---

## Подсказки для ревью

- Клиентский `handleStartGame` по-прежнему локально включает экран и отправляет
  `room:show-qr { show: true }`; серверный initial snapshot теперь согласован с
  этими действиями и не может позднее закрыть экран.
