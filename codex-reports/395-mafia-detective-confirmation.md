# REPORT TASK-395: Подтверждение выбора Шерифа

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-09
> - **Финиш:** 2026-08-09
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Ночное действие Шерифа переведено на тот же двухшаговый выбор, что и действие
Доктора. Нажатие на игрока только открывает подтверждение; действие передаётся
в игру после отдельной кнопки «Подтвердить».

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/mafia/page.tsx` — добавлено локальное состояние
  предварительного выбора Шерифа, экран подтверждения с кнопками «Назад» и
  «Подтвердить», а также сброс выбора при смене этапа.
- `codex-reports/CODEX-HANDOFF.md` — добавлена TASK-395 и следующий свободный
  номер TASK-396.

### Новые файлы

- `codex-reports/395-mafia-detective-confirmation.md` — этот отчёт.

### Удалённые файлы

- Нет.

---

## Diff stat

```text
1 production-файл изменён.
Точный task-only stat недоступен из-за незакоммиченных изменений TASK-385–394
в том же файле.
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ✅ | Ветка актуальна |
| `npx tsc --noEmit` | ✅ | Ошибок нет |
| `npm run lint` | ✅ | 0 ошибок; 4 прежних warning в legacy Mafia render-функциях |
| `git diff --check` | ✅ | Ошибок нет |
| HTTP smoke `/game/N85Q4Q/mafia` | ✅ | 200 после HMR |
| `npm run build` | ⏭️ | Не запускался, чтобы не перезапускать активную комнату `N85Q4Q` |

---

## Отклонения от ТЗ

Нет.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано

- Production build отложен до завершения живого прогона комнаты.

---

## Подсказки для ревью

- `handleDetectiveCheck` вызывается только кнопкой подтверждения, не первым
  нажатием на игрока.
- Выбор сбрасывается вместе с выборами Доктора и дневного голосования при
  смене фазы, ночного этапа или раунда.
