# REPORT TASK-160.1: Подключить useNavigateOnGameStart в TV-лобби страницу

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-27 20:46
> - **Финиш:** 2026-05-27 20:51
> - **Длительность:** 5 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

TV-лобби страница теперь использует общий `useNavigateOnGameStart` вместо локального `game:started` listener'а. `room:state` listener отделён в отдельный `useEffect`, `useRouter` удалён из файла.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/page.tsx` — добавлен импорт `useNavigateOnGameStart`, удалён прямой `game:started` listener и неиспользуемый `useRouter`; `room:state` listener оставлен отдельным эффектом.

### Новые файлы

- `codex-reports/160.1-game-started-tv-lobby-page.md` — отчёт по выполнению TASK-160.1.

### Удалённые файлы

- (нет)

---

## Diff stat

Scoped на whitelist-файл задачи:

```text
 src/app/tv/[roomId]/page.tsx | 22 ++++++++--------------
 1 file changed, 8 insertions(+), 14 deletions(-)
```

Примечание: в рабочем дереве уже есть изменения/новые файлы от TASK-160 (`src/app/join/[code]/page.tsx`, `src/components/lobby/Lobby.tsx`, `src/lib/use-navigate-on-game-start.ts`, `codex-tasks/160*`, `codex-reports/160*`). Я их не редактировал в TASK-160.1.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ✅ | Already up to date. |
| `grep -RIn "game:started" src/app/ src/components/` | ✅ | 0 совпадений; команда вернула exit code 1 без вывода, что для `grep` означает "not found". |
| `npm run lint` | ✅ | Без ошибок. |
| `npx tsc --noEmit` | ✅ | Без ошибок. |
| `npm run build` | ⚠️ | Turbopack internal error из-за sandbox: `creating new process` / `binding to a port` / `Operation not permitted` при обработке `geist` CSS. |

---

## Отклонения от ТЗ

Нет отклонений по коду. Дополнительно запускал `npm run build` по проектному workflow; он упал на sandbox-ограничении Turbopack, не на TypeScript/lint ошибке.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверь `src/app/tv/[roomId]/page.tsx`: прямой listener удалён, навигация теперь идёт через общий hook с TV path resolver.
