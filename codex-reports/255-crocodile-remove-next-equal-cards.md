# REPORT TASK-255: Крокодил — убрать host «Следующий игрок» + выровнять красные карточки

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-06-17 19:55
> - **Финиш:** 2026-06-17 20:00
> - **Длительность:** 5 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Удалена кнопка хоста «Следующий игрок →» из мобильного экрана Крокодила. Две красные карточки для не-объясняющего приведены к тем же габаритам, что и карточки объясняющего: `min-h-[320px]`, `max-w-md`, `flex-1`, `rounded-[36px]`.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/crocodile/page.tsx` — удалён блок host-only кнопки принудительного перехода к следующему игроку; карточки `ready && !isExplainer` и `explaining && !isExplainer` выровнены по размерам с карточками объясняющего.

### Новые файлы

- `codex-reports/255-crocodile-remove-next-equal-cards.md` — отчёт по TASK-255.

### Удалённые файлы

- (нет)

---

## Diff stat

```text
src/app/game/[roomId]/crocodile/page.tsx | 341 ++++++++++++++++++-------------
1 file changed, 198 insertions(+), 143 deletions(-)
```

Примечание: stat по файлу включает незакоммиченные изменения TASK-253/254, которые уже были в рабочем дереве до TASK-255. Diff TASK-255 поверх текущего состояния: удалён блок кнопки «Следующий игрок» и изменены два `className`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | без ошибок |
| `npx tsc --noEmit` | ✅ | без ошибок |
| `npm run build` | — | не запускал по ТЗ |
| Acceptance: нет кнопки host «Следующий игрок» | ✅ | `grep` не находит `Следующий игрок`, `Next Player`, `Host can force` |
| Acceptance: 4 красные карточки одного размера | ✅ | все 4 карточки имеют `min-h-[320px]`, `w-full max-w-md`, `flex-1`, `rounded-[36px]` |

---

## Отклонения от ТЗ

Нет отклонений по реализации. В рабочем дереве до старта уже были изменения вне whitelist (`src/app/globals.css`, `src/app/tv/[roomId]/[gameType]/page.tsx`, `src/components/ui/PlayerAvatar.tsx`) и незакоммиченные изменения в целевом файле; я их не откатывал и не редактировал.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверить в `src/app/game/[roomId]/crocodile/page.tsx` участок ready/explaining: блок host-only `GlassButton` удалён, а карточки non-explainer получили такие же размерные классы, как explainer-карточки.
