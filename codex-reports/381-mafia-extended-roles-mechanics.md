# REPORT TASK-381: Реализовать роли Дон/Маньяк/Любовница в геймплее Мафии

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-27 21:50
> - **Финиш:** 2026-07-27 22:58
> - **Длительность:** 68 минут
> - **Статус:** ⚠️ partial

---

## Резюме (TL;DR)

Реализованы новые роли Мафии: Дон, Маньяк, Любовница, их ночные действия,
блокировки, алиби Любовницы, смерть цели Любовницы при её ночной смерти и TV
отображение победы Маньяка. Единственный сознательно не закрытый пункт —
точное условие соло-победы Маньяка: в `CLAUDE.md` оно не задано, а ТЗ прямо
запрещает додумывать его самостоятельно.

---

## Что сделано

### Изменённые файлы

- `src/types/game.ts` — расширен `MafiaRole`, добавлены поля ночных действий и
  истории для Мафии, расширен `winner` до варианта `maniac`.
- `src/app/game/[roomId]/mafia/page.tsx` — добавлены раздача Дона/Маньяка с 10
  игроков и Любовницы с 12, новые broadcast action'ы, UI ночных действий,
  host-side resolution ночи по спецификации, алиби Любовницы, несколько ночных
  смертей, блокировка Любовницей и TV/mobile winner text для Маньяка.
- `src/app/tv/[roomId]/[gameType]/page.tsx` — Mafia TV теперь понимает
  множественные ночные смерти и `winner: 'maniac'`.

### Новые файлы

- `codex-reports/381-mafia-extended-roles-mechanics.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/game/[roomId]/mafia/page.tsx    | 395 +++++++++++++++++++++++++++-----
 src/app/tv/[roomId]/[gameType]/page.tsx |  22 +-
 src/types/game.ts                       |  10 +-
 3 files changed, 367 insertions(+), 60 deletions(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | ESLint clean |
| `npx tsc --noEmit` | ✅ | TypeScript clean |
| `npm run build` | ❌ | Turbopack sandbox panic: `creating new process / binding to a port / Operation not permitted` на `geistmono` CSS |
| `npx next build --webpack` | ✅ | Build successful, exit code 0; во время prerender `/profile` есть существующий `ReferenceError: location is not defined` |
| 4-9 игроков | ✅ | Раздача ролей не расширяется; ограничения доктора на self/same включаются только при наличии новых ролей |
| 10+ игроков | ✅ | Раздаются Дон и Маньяк |
| 12+ игроков | ✅ | Раздаётся Любовница |
| Ночной порядок | ✅ | Mafia+Don → Lover block → Maniac → Doctor → Sheriff → Don check |
| Алиби Любовницы | ✅ | Дневное изгнание цели Любовницы отменяется |
| Смерть Любовницы | ✅ | Если Любовницу убили ночью, её цель тоже добавляется в killedIds |
| Победа Маньяка | ⚠️ | UI/types готовы, но trigger не реализован из-за открытого условия победы |

---

## Отклонения от ТЗ

- Не реализован триггер `game-over: maniac`, потому что точное условие победы
  Маньяка отсутствует в `CLAUDE.md`, а ТЗ прямо говорит не придумывать его.
- `npm run build` не прошёл из-за Turbopack/sandbox-ошибки окружения. Fallback
  `npx next build --webpack` прошёл.

---

## Открытые вопросы для Claude

- Какое точное условие соло-победы Маньяка?
  Вариант A: Маньяк побеждает, когда остаётся единственным живым игроком.
  Вариант B: Маньяк побеждает, когда жив и все мафиози выбыли, а мирных осталось
  не больше одного.
- Подтвердить интерпретацию Дона: его голос по жертве мафии полностью решающий
  при любом разногласии, а не только при равном числе голосов.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

- Не активирована соло-победа Маньяка до продуктового решения по условию.

---

## Подсказки для ревью

- Обратить внимание на `handleResolveNight()` в
  `src/app/game/[roomId]/mafia/page.tsx`: там весь порядок ночного resolution.
- Обратить внимание на сохранение старого 4-ролевого поведения доктора:
  запрет лечить себя/ту же цель включается только при наличии расширенных ролей.
- В рабочем дереве до/во время задачи уже были изменения вне whitelist:
  `codex-tasks/_DONE.md`, `src/app/admin/page.tsx`,
  `src/app/api/admin/game-data/route.ts`, `codex-tasks/380...`,
  `codex-tasks/381...`, `public/icons/mafia-roles/`. Я их не редактировал.
