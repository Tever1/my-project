# REPORT TASK-347: «100 к 1» — TV-счётчики ошибок по бокам ответов

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-13 21:58
> - **Финиш:** 2026-07-13 22:03
> - **Длительность:** 5 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Счётчики ошибок в TV-игре «100 к 1» перенесены из нижней панели в боковые колонки слева/справа от ответов. Логика `h.strikes[h.curQ][0/1]` не менялась; нижняя панель теперь показывает только банк или таймер обсуждения.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — в playing-блоке `hundred-to-one` колонка ответов обёрнута в flex-ряд с боковыми strike-колонками для команд 1/2; нижняя панель очищена от крестиков.

### Новые файлы

- `codex-reports/347-h2o-tv-strikes-sidebars.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/tv/[roomId]/[gameType]/page.tsx | 98 ++++++++++++++++++++-------------
 1 file changed, 61 insertions(+), 37 deletions(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| `npm run build` | ❌ | Turbopack internal error в sandbox: `creating new process` / `binding to a port` / `Operation not permitted`; сборка упала до проверки кода задачи. |
| Acceptance #1 | ✅ | `lint` и `tsc` чисто. |
| Acceptance #2 | ✅ | Крестики удалены из нижней панели. |
| Acceptance #3 | ✅ | Для `h.curQ === 3` боковые колонки не рендерятся, остаётся `flex-1` колонка ответов. |
| Acceptance #4 | ✅ | Подсчёт `h.strikes[h.curQ][0/1]` не менялся. |

---

## Отклонения от ТЗ

Нет отклонений по коду. Дополнительно был запущен `npm run build` по проектному workflow; он упал из-за ограничения sandbox/Turbopack, не из-за TypeScript или ESLint.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Посмотреть только блок playing-фазы `hundred-to-one` в `src/app/tv/[roomId]/[gameType]/page.tsx`: боковые колонки находятся под условием `h.curQ <= 2`, нижняя панель больше не содержит strike UI.
- В рабочем дереве до старта уже были untracked task/report файлы TASK-346/TASK-347 и незакоммиченная правка TASK-346 в этом TV-файле; я их не откатывал.
