# REPORT TASK-368: TV «100 к 1» Большая игра — не показывать все вопросы во время выбора игроков и ответов

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-20 22:00 PDT
> - **Финиш:** 2026-07-20 22:02 PDT
> - **Длительность:** 2 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В TV-рендере «100 к 1» для `bigGame` добавлен отдельный экран ожидания на `bgPhase === 0`, без списка вопросов, таймера и фонда. Для фаз ответа `bgPhase === 1` и `bgPhase === 3` список вопросов фильтруется до текущего `h.bgCurQ`; фазы проверки `2` и `4` оставлены с полным списком.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — в блоке `h.phase === 'bigGame'` добавлен ternary на `h.bgPhase === 0`; для `activeBigQ.map` добавлен ранний `return null` только в фазах ответа игрока.

### Новые файлы

- `codex-reports/368-h2o-tv-biggame-hide-all-questions.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff по строкам

- `src/app/tv/[roomId]/[gameType]/page.tsx:1582` — `bigGame` теперь ветвится на `h.bgPhase === 0` и остальные подфазы.
- `src/app/tv/[roomId]/[gameType]/page.tsx:1583-1602` — новый центрированный экран «Капитан выбирает игроков…» в стиле `topicSelect`.
- `src/app/tv/[roomId]/[gameType]/page.tsx:1605-1614` — прежняя заголовочная плашка сохранена для `bgPhase !== 0`.
- `src/app/tv/[roomId]/[gameType]/page.tsx:1618` — в `activeBigQ.map` добавлен фильтр: в `bgPhase === 1 || bgPhase === 3` рендерится только `i === h.bgCurQ`.

---

## Diff stat

```
src/app/tv/[roomId]/[gameType]/page.tsx | 666 +++++++++++++++++++++++---------
1 file changed, 494 insertions(+), 172 deletions(-)
```

Примечание: stat выше взят из текущего `git diff` по файлу и включает уже существовавшие незакоммиченные изменения в этом файле до TASK-368. Мой фактический diff ограничен строками блока `bigGame`, перечисленными выше.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npx tsc --noEmit` | ✅ | без ошибок |
| `npm run lint` | ✅ | без warnings/errors |
| Acceptance: `bgPhase === 0` | ✅ | TV показывает ожидание выбора игроков вместо вопросов/сайдбара |
| Acceptance: `bgPhase === 1` или `3` | ✅ | рендерится только текущий вопрос `i === h.bgCurQ`, сайдбар сохранён |
| Acceptance: `bgPhase === 2` или `4` | ✅ | полный список вопросов не фильтруется |
| Acceptance: `bgPhase === 5` / `final` | ✅ | не затронуто |

---

## Отклонения от ТЗ

Нет отклонений. Кодовая правка сделана только в `src/app/tv/[roomId]/[gameType]/page.tsx`; отчёт создан по явному требованию задачи.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано

Ничего.

---

## Подсказки для ревью

- Проверить `src/app/tv/[roomId]/[gameType]/page.tsx:1583-1618`: ветвление `bgPhase === 0` должно полностью обходить старый header/grid, а фильтр вопросов должен работать только для фаз ответа `1` и `3`.
