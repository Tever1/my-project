# REPORT TASK-053: Auth buttons glass

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-10 13:27 PDT
> - **Финиш:** 2026-05-10 13:28 PDT
> - **Длительность:** 1 минута
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Кнопки auth modal стали более glass-like: primary «Войти» теперь белая с opacity `0.75`, secondary «Назад» — с opacity `0.20`. Цвета текста, рамка и остальные стили не менялись.

---

## Что сделано

### Изменённые файлы

- `src/components/lobby/Lobby.tsx` — обновлена строка `background` в `btnStyle`: primary `0.9 → 0.75`, secondary `0.12 → 0.20`.

### Новые файлы

- `codex-reports/053-auth-buttons-glass.md` — отчёт по TASK-053.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/components/lobby/Lobby.tsx | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | без ошибок |
| `npx tsc --noEmit` | ✅ | без ошибок |
| «Войти» прозрачнее | ✅ | `rgba(255,255,255,0.75)` |
| «Назад» заметнее | ✅ | `rgba(255,255,255,0.20)` |

---

## Отклонения от ТЗ

нет отклонений.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

—

---

## Подсказки для ревью

- Проверить одну строку `background` в `btnStyle`; остальные свойства намеренно не изменены.
