# REPORT TASK-360: «100 к 1» mobile — убрать лишний зазор между «Тема:» и названием темы

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-15 23:23
> - **Финиш:** 2026-07-15 23:27
> - **Длительность:** 4 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Исправлен зазор между подписью «Тема:» и названием темы на mobile title-экране «100 к 1». Общий `tracking-[4px]` заменён на два `span` с отдельной разрядкой и контролируемым `gap-1.5` между ними.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/hundred-to-one/page.tsx` — в title-фазе строка темы разбита на два `span`; `letter-spacing` больше не применяется к пробелу между label и значением.

### Новые файлы

- `codex-reports/360-h2o-mobile-title-topic-gap-fix.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/game/[roomId]/hundred-to-one/page.tsx | 873 +++++++++++++++-----------
 1 file changed, 510 insertions(+), 363 deletions(-)
```

Примечание: stat большой из-за уже существующих незакоммиченных изменений в этом файле до TASK-360. Собственная правка TASK-360 ограничена блоком строки с `Тема:`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| Acceptance #1 | ✅ | lint и tsc чисто |
| Acceptance #2 | ✅ | зазор теперь задаётся `gap-1.5`, не letter-spacing на пробеле |
| Acceptance #3 | ✅ | коммит не делал |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверить только title-блок в `src/app/game/[roomId]/hundred-to-one/page.tsx`: `<p>` теперь `flex ... gap-1.5`, оба дочерних `span` сохраняют `tracking-[4px]`.
