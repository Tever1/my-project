# REPORT TASK-141: add invite button room menu

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-25 19:50
> - **Финиш:** 2026-05-25 19:55
> - **Длительность:** 5 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В RoomMenu добавлена host-only кнопка «Пригласить игрока». Она копирует ссылку вида `/join/{roomCode}` и на 2 секунды меняет текст на «Ссылка скопирована ✓».

---

## Что сделано

### Изменённые файлы

- `src/components/lobby/Lobby.tsx` — добавлены `inviteUrl`, обработчик копирования через Clipboard API, временное состояние copied и кнопка приглашения в QR-секции RoomMenu.

### Новые файлы

- `codex-reports/141-add-invite-button-room-menu.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/components/lobby/Lobby.tsx | 45 ++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 45 insertions(+)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | ESLint прошёл без ошибок |
| `npm run build` | ⚪ | Не запускался: в ТЗ acceptance указан lint |
| Acceptance: кнопка видна только host | ✅ | Рендер под условием `isCurrentUserHost && inviteUrl` |
| Acceptance: копируется `/join/{roomCode}` | ✅ | QR `joinUrl` оставлен на `/lobby/{roomCode}`, invite-кнопка использует отдельный `inviteUrl` |
| Acceptance: текст меняется на 2 секунды | ✅ | После успешного copy включается `inviteCopied`, затем сбрасывается таймером |

---

## Отклонения от ТЗ

Нет отклонений. Кнопка добавлена в QR-секцию RoomMenu, поскольку в текущей структуре файла список игроков уже расположен выше QR-блока.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

- (нет)

---

## Подсказки для ревью

- Проверь, что новая кнопка копирует именно `/join/{roomCode}`, а существующий QR продолжает использовать `/lobby/{roomCode}`.
