# REPORT TASK-011.1: Press-effect на тайле через variant

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-03 21:59 PDT
> - **Финиш:** 2026-05-03 22:03 PDT
> - **Длительность:** 4 минуты
> - **Статус:** ⚠️ partial

---

## Резюме (TL;DR)

Фикс TASK-011 внесён: press-effect теперь идёт через string variant `whileTap="tap"` на корневом `motion.button`, а реальная компрессия `scale: 0.95` находится во внутреннем tile `motion.div`. Код целевого файла проходит isolated ESLint; полный lint/build не зелёные по уже существующим ошибкам вне whitelist и sandbox-ограничению Turbopack.

---

## Что сделано

### Изменённые файлы

- `src/app/lobby-preview/page.tsx` — в `function Tile` заменён `whileTap={{ scale: 0.97 }}` на `whileTap="tap"`; во внутренний tile `motion.div` добавлен variant `tap: { y: -1, scale: 0.95 }`.

### Новые файлы

- `codex-reports/011.1-tile-tap-variant.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

Текущий полный diff включает незакоммиченные изменения предыдущих TASK-010.* / TASK-011 и `.codex/STATUS.md`, которые уже были в worktree до TASK-011.1:

```text
 .codex/STATUS.md               |   7 +-
 src/app/lobby-preview/page.tsx | 157 +++++++++++++++++++++++++++++++++++++----
 2 files changed, 149 insertions(+), 15 deletions(-)
```

Целевой diff TASK-011.1 в `src/app/lobby-preview/page.tsx`:

```tsx
whileTap="tap"
tap: { y: -1, scale: 0.95 },
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ❌ | Sandbox: `error: cannot open '.git/FETCH_HEAD': Operation not permitted` |
| `npx eslint src/app/lobby-preview/page.tsx` | ✅ | Целевой файл без ошибок |
| `npm run lint` | ❌ | Падает на существующих ошибках вне whitelist (`.agents`, `mobile`, `src/app/admin`, игровые страницы, `src/lib/*`); ошибок по `src/app/lobby-preview/page.tsx` нет |
| `npm run build` | ❌ | Turbopack internal error из-за sandbox: `creating new process`, `binding to a port`, `Operation not permitted` |
| Acceptance #1: `npm run build` успешен | ❌ | Не подтверждено из-за sandbox/Turbopack |
| Acceptance #2: клик/тап даёт заметную компрессию | ✅ по коду | Parent задаёт `whileTap="tap"`, child tile получает `scale: 0.95` |
| Acceptance #3: hover/active/focus не сломано | ✅ по коду | `rest/hover/active`, `whileHover`, `animate`, focus handlers сохранены |

---

## Отклонения от ТЗ

- Halo-`motion.div` не менялся; опциональный `tap: { opacity: 0.4 }` не добавлял, чтобы сохранить diff минимальным.
- `STATUS.md` всё ещё показывает активный TASK-010 с lock на `src/app/lobby-preview/page.tsx`. Продолжил, потому что пользователь явно попросил выполнить TASK-011.1, а в worktree уже есть отчёты TASK-010.* и TASK-011.
- `npm run build` не прошёл из-за ограничения окружения, а не из-за изменения TASK-011.1.
- Полный `npm run lint` не зелёный из-за существующих ошибок вне whitelist.

---

## Открытые вопросы для Claude

- Нужно ли обновить `.codex/STATUS.md`: сейчас там всё ещё активен TASK-010, хотя в worktree уже есть отчёты TASK-010.* / TASK-011 и выполнен TASK-011.1.
- Нужно ли повторить `npm run build` вне sandbox, где Turbopack сможет создавать процесс/порт.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

- Не подтверждён зелёный `npm run build` из-за sandbox/Turbopack `Operation not permitted`.
- Не проводился ручной browser QA; acceptance по tap-effect проверен по коду.

---

## Подсказки для ревью

- Основной участок ревью: `src/app/lobby-preview/page.tsx` внутри `function Tile`, рядом с `whileHover="hover"` и variants внутреннего tile `motion.div`.
