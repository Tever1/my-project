# REPORT TASK-383: Концепции редизайна Мафии

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-04 19:50 PDT
> - **Финиш:** 2026-08-04 21:36 PDT
> - **Длительность:** 106 минут (включая восстановление после потери соединения)
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Создана отдельная внутренняя страница `/mafia-design-preview` с тремя
визуальными направлениями редизайна Мафии. В каждом направлении показаны два
утверждённых макета: мобильное раскрытие роли удерживанием и ночная фаза TV в
реальном соотношении 1920×1080; игровая логика не изменялась.

---

## Что сделано

### Изменённые файлы

- (нет)

### Новые файлы

- `src/app/mafia-design-preview/page.tsx` — интерактивная preview-страница с
  концепциями «Кинематографичный нуар», «Закрытый клуб» и «Фиолетовый
  триллер»; телефон 390×844, TV 1920×1080, выбор направления и press-and-hold
  раскрытие полной исходной карточки роли.
- `codex-reports/383-mafia-design-preview.md` — этот отчёт.

### Удалённые файлы

- (нет)

---

## Diff stat

```text
src/app/mafia-design-preview/page.tsx | 546 строк, 40957 байт (новый файл)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | Ошибок нет |
| `npx tsc --noEmit` | ✅ | Ошибок нет |
| `npm run build` | ✅ | Exit code 0; `/mafia-design-preview` пререндерится как static route. В логе остаётся существующее сообщение `ReferenceError: location is not defined`, сборку оно не ломает |
| Desktop visual QA, 1440×1000 | ✅ | Колода и две реальные рамки устройств читаются корректно |
| Small phone QA, 375×812 | ✅ | Горизонтальной прокрутки нет (`scrollWidth 369` при viewport 375) |
| Landscape QA, 812×375 | ✅ | Горизонтальной прокрутки нет (`scrollWidth 806` при viewport 812) |
| Выбор концепции | ✅ | Кнопка меняет `aria-pressed`, итоговый блок показывает выбранный вариант |
| Browser console | ✅ | Ошибок нет |
| Reduced motion | ✅ | Учтён `useReducedMotion` и `prefers-reduced-motion` |

---

## Отклонения от ТЗ

Нет. Это только внутренняя русскоязычная preview-страница; production-экраны
Мафии и игровая логика намеренно не менялись до выбора концепции.

---

## Открытые вопросы для Claude

- После выбора пользователем одного из трёх направлений нужно создать
  отдельную задачу на раскатку выбранного визуального языка на все фазы mobile
  и TV.
- При следующем техническом проходе можно отдельно исследовать существующее
  build-сообщение `ReferenceError: location is not defined`; оно не связано с
  TASK-383 и сейчас не влияет на успешный exit code.

---

## Что НЕ сделано

- Полный production-редизайн Мафии не начат: ожидается выбор концепции.
- Сохранение выбора между перезагрузками не добавлялось: кнопки предназначены
  только для обсуждения макета.

---

## Подсказки для ревью

- Вся реализация TASK-383 изолирована в
  `src/app/mafia-design-preview/page.tsx`.
- Карточки загружаются напрямую из `public/icons/mafia-roles/`; изображения не
  модифицировались.
- TV-холст имеет внутренний размер 1920×1080 и масштабируется только внешней
  рамкой preview.
- Раскрытие роли доступно указателем и клавиатурой и скрывается при отпускании,
  отмене или уходе указателя.
