# TASK-499 — Исправления по независимому review перед push

## Результат

Перед публикацией совокупного commit `cb74947` отдельный Codex review нашёл два подтверждённых дефекта realtime/reconnect. Оба исправлены до push:

- все игровые действия теперь сначала материализуют прошедшее серверное время и только затем проходят permission/phase validation; просроченный ответ Quiz больше не принимается первым событием после паузы клиента;
- страница подключения помечает игрока присоединённым только после успешного `room:join` acknowledgement. При отсутствующем или устаревшем reconnect-token остаётся доступен обычный join flow с сообщением об ошибке; повторная автоматическая попытка для той же пары room/player не зацикливается.

## Изменённые файлы

- `src/server/socket-handlers.mts`
- `src/server/reconnect.integration.test.mts`
- `src/app/join/[code]/page.tsx`
- `src/lib/join-reconnect-flow.test.ts`

## Проверки

- Новый Socket.io regression воспроизводит просроченный ответ Quiz и подтверждает, что сервер показывает результат без принятия ответа.
- Новый source regression подтверждает, что `joined=true` устанавливается только внутри successful acknowledgement branch.
- Scoped run: 14/14 tests passed.
- Полный TypeScript test run: 95/95 passed.
- `npx tsc --noEmit`: passed.
- `npm run lint`: 0 errors; сохранены четыре известных предупреждения о неиспользуемых legacy renderer Мафии.
- Повторный независимый review выполняется как последний pre-push gate после commit исправления.

## Отклонённое замечание review

Review предложил скрыть от телефона персонажей остальных игроков в Who Am I. Это противоречит утверждённому правилу: `PROJECT_CONTEXT.md` и `docs/who-am-i-design-brief.md` требуют скрывать только собственного персонажа, а персонажей остальных показывать игроку для ответов «Да/Нет». Существующие privacy-тесты именно это проверяют. Код не изменён; TV по-прежнему получает персонажей только в разрешённом итоговом состоянии.

## Не выполнялось

Browser/multiplayer QA и production build не запускались: отдельного разрешения на них в этой публикации не было. Push выполняется только после успешных финальных проверок и повторного review.
