# REPORT TASK-381.1: Победа Маньяка, голос Дона, ничья на голосовании

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-27 23:00
> - **Финиш:** 2026-07-27 23:15
> - **Длительность:** 15 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Реализованы три уточнения поверх TASK-381: соло-победа Маньяка, голос Дона как tie-breaker только при разногласии рядовой мафии, и трёхраундовый протокол дневной ничьей. Mobile и TV теперь отображают переголосование и бинарное голосование.

---

## Что сделано

### Изменённые файлы

- `src/types/game.ts` — добавлены типы и поля состояния для раундов дневного голосования, списка кандидатов и результата последнего vote-resolution.
- `src/app/game/[roomId]/mafia/page.tsx` — добавлен trigger победы Маньяка, исправлен расчёт цели мафии с голосом Дона, реализованы раунды 1/2/3 дневного голосования, алиби Любовницы применяется при обычной и бинарной казни, обновлены mobile voting/results экраны.
- `src/app/tv/[roomId]/[gameType]/page.tsx` — Mafia TV больше не схлопывает `voting` в `day`, показывает переголосование и бинарный раунд, понимает новые события `vote-tie`, `vote-pardoned`, `eliminate-many`.

### Новые файлы

- `codex-reports/381.1-mafia-maniac-win-don-tiebreak-vote-tiebreak.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/game/[roomId]/mafia/page.tsx    | 705 +++++++++++++++++++++++++++-----
 src/app/tv/[roomId]/[gameType]/page.tsx | 103 ++++-
 src/types/game.ts                       |  16 +-
 3 files changed, 708 insertions(+), 116 deletions(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | ESLint clean |
| `npx tsc --noEmit` | ✅ | TypeScript clean |
| `git diff --check` | ✅ | Whitelist-файлы без whitespace errors |
| `npm run build` | ❌ | Известный Turbopack sandbox panic: `creating new process / binding to a port / Operation not permitted` на `geist` CSS |
| `npx next build --webpack` | ✅ | Build successful; сохранился существующий prerender warning `ReferenceError: location is not defined` на `/profile`, exit code 0 |
| Маньяк один живой | ✅ | `checkWin()` возвращает `maniac` до проверки победы мирных |
| Голос Дона | ✅ | Рядовая мафия с чистым лидером не перебивается; при tie/split используется голос Дона |
| Раунд 2 при ничьей | ✅ | `vote-tie` с `round: 2`, список целей сужен до tied candidates |
| Раунд 3 при повторной ничьей | ✅ | `vote-tie` с `round: 3`, UI показывает `execute` / `pardon` |
| Третья ничья | ✅ | `executeVotes <= pardonVotes` оправдывает кандидатов, никто не выбывает |
| Алиби Любовницы | ✅ | Цель Любовницы не выбывает в обычном голосовании и исключается из binary execution |

---

## Отклонения от ТЗ

Нет отклонений по gameplay. После оправдания кандидатов игра остаётся на results-экране с кнопкой ведущего "Следующая ночь", как и существующий outcome алиби/казни; авто-переход в ночь не добавлялся.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Ничего.

---

## Подсказки для ревью

- Обратить внимание на `handleResolveVotes()` — там вся новая state-machine логика раундов 1/2/3.
- Обратить внимание на `handleResolveNight()` — изменён только выбор mafia target: Дон теперь не перебивает единственного лидера среди обычной мафии.
- В рабочем дереве до задачи уже были изменения вне whitelist: `CLAUDE.md`, `codex-tasks/_DONE.md`, `src/app/admin/page.tsx`, `src/app/api/admin/game-data/route.ts`, task-файлы и `public/icons/mafia-roles/`. Я их не редактировал.
