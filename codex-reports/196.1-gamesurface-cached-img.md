# REPORT TASK-196.1: GameSurface cached image load

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-06-02 21:04
> - **Финиш:** 2026-06-02 21:08
> - **Длительность:** 4 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В `GameSurface` добавлен callback ref для полного фонового `<img>`, который обрабатывает cached-load случай через `complete + naturalWidth`. Сетевой сценарий через `onLoad`, LQIP-плейсхолдер, fade-in transition и `isolate`/`-z-10` сохранены.

---

## Что сделано

### Изменённые файлы

- `src/components/games/GameSurface.tsx` — добавлен `handleBackgroundImageRef`, который при монтировании уже загруженной картинки выставляет `loadedBackgroundUrl = backgroundUrl`.

### Новые файлы

- `codex-reports/196.1-gamesurface-cached-img.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
src/components/games/GameSurface.tsx | 51 ++++++++++++++++++++++++++++++------
1 file changed, 43 insertions(+), 8 deletions(-)
```

Примечание: stat по `GameSurface.tsx` включает ранее существующие незакоммиченные изменения TASK-196; для TASK-196.1 добавлен только cached-load ref-callback.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| Фон виден при сетевой загрузке | ✅ | `onLoad` оставлен без изменений |
| Фон виден при cached загрузке | ✅ | ref-callback проверяет `node.complete && node.naturalWidth > 0` |
| Плейсхолдер до загрузки нового фона | ✅ | `backgroundLoaded` остаётся привязан к текущему `backgroundUrl` |

---

## Отклонения от ТЗ

нет отклонений.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

не применимо.

---

## Подсказки для ревью

- Проверь `src/components/games/GameSurface.tsx`: ref-callback использует тот же setter, что и `onLoad`, и зависит от `backgroundUrl`, поэтому при смене URL старый loaded state не делает новый фон видимым заранее.
