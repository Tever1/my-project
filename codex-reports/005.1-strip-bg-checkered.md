# REPORT TASK-005.1: Strip checkered background pixels

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-03 11:43
> - **Финиш:** 2026-05-03 11:44
> - **Длительность:** 1 минута
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Логика `scripts/strip-bg.mjs` обновлена: теперь фон определяется как светлый и ахроматический пиксель, а не только как чисто-белый. Скрипт генерации/обработки PNG не запускался, изображения не изменялись в рамках этого таска.

---

## Что сделано

### Изменённые файлы

- `scripts/strip-bg.mjs` — `WHITE_THRESHOLD` заменён на `LIGHT_THRESHOLD` и `CHROMA_THRESHOLD`; `stripWhitePixels()` переименована в `stripBackgroundPixels()`; условие стрипа теперь использует `min(R,G,B) >= 220` и `max-min <= 15`.

### Новые файлы

- `codex-reports/005.1-strip-bg-checkered.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

Обычный `git diff --stat -- scripts/strip-bg.mjs` не показывает файл, потому что `scripts/strip-bg.mjs` сейчас untracked после TASK-005. Для целевого файла:

```
/dev/null => scripts/strip-bg.mjs | 66 +++++++++++++++++++++++++++++++++++++++
1 file changed, 66 insertions(+)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ❌ | Sandbox: `error: cannot open '.git/FETCH_HEAD': Operation not permitted` |
| `node --check scripts/strip-bg.mjs` | ✅ | Без ошибок |
| `npm run lint` | ❌ | Падает на существующих ошибках вне whitelist (`.agents`, `mobile`, `src/*`); новых сообщений по `scripts/strip-bg.mjs` нет |
| `npm run build` | ❌ | Turbopack упал из-за sandbox: `binding to a port / Operation not permitted` |
| Acceptance #1 | ✅ | `node --check scripts/strip-bg.mjs` прошёл |
| Acceptance #2 | ⚠️ | `npm run lint` запущен, но репозиторий уже содержит 73 lint problems вне whitelist |
| Acceptance #3 | ✅ | Логика сохраняет idempotency: пиксели с `a === 0` не трогаются |
| Acceptance #4 | ⏭️ | Не проверялось: по ТЗ `npm run strip-bg` не запускать |

---

## Отклонения от ТЗ

- `git pull` не удалось выполнить из-за sandbox-ограничения на `.git/FETCH_HEAD`.
- `npm run build` был запущен как стандартная проверка проекта, но завершился Turbopack internal error из-за sandbox-ограничения на создание процесса/порт.
- Скрипт `npm run strip-bg` не запускался, PNG не трогались.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

---

## Подсказки для ревью

- Проверь условие в `scripts/strip-bg.mjs`: серые клетки шахматки `[233,232,233]` и белые `[253,253,253]` теперь попадают под фон, а цветные highlights с большим spread между каналами не должны стрипаться.
