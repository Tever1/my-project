# REPORT TASK-384: Полный набор макетов «Закрытого клуба»

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-05 19:35 PDT
> - **Финиш:** 2026-08-05 20:42 PDT
> - **Длительность:** 67 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

После выбора пользователем направления «Закрытый клуб» внутренняя страница
`/mafia-design-preview` расширена шестью интерактивно переключаемыми парами
макетов mobile + TV. Production-экраны и игровая логика Мафии не менялись.

---

## Что сделано

### Изменённые файлы

- `src/app/mafia-design-preview/page.tsx` — добавлена полная история игры в
  стиле «Закрытого клуба»: сбор клуба, ночное действие, утро, голосование,
  приговор и финал. Для каждой фазы есть телефон 390×844 и TV 1920×1080.
- `codex-reports/CODEX-HANDOFF.md` — точка остановки обновлена до TASK-384.

### Новые файлы

- `codex-reports/384-mafia-club-full-flow-preview.md` — этот отчёт.

### Удалённые файлы

- (нет)

---

## Diff stat

```text
src/app/mafia-design-preview/page.tsx | +313 строк относительно TASK-383
итоговый размер preview-файла          | 859 строк, 77308 байт
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | Ошибок нет |
| `npx tsc --noEmit` | ✅ | Ошибок нет |
| `npm run build` | ✅ | Exit code 0, `/mafia-design-preview` — static route; существующее нефатальное сообщение `location is not defined` осталось |
| Все 6 переключателей фаз | ✅ | Каждая пара экранов отрендерилась, `aria-pressed` обновляется |
| Browser console | ✅ | Ошибок нет |
| Small phone, 375×812 | ✅ | `scrollWidth 369`, боковой прокрутки нет |
| Landscape, 812×375 | ✅ | `scrollWidth 806`, боковой прокрутки нет |
| Reduced motion | ✅ | Переходы используют `useReducedMotion`; CSS-пульсация отключается через `prefers-reduced-motion` |

---

## Отклонения от ТЗ

Нет. Новые элементы находятся только на внутренней preview-странице.

---

## Открытые вопросы для Claude

- Перед production-портом пользователь должен утвердить эти шесть пар экранов
  и решить, нужны ли отдельные полноразмерные макеты для веток: Доктор спас,
  блокировка Любовницей, ничья/переголосование, казнить/помиловать, наблюдение
  выбывшего, победа Мафии и победа Маньяка.
- Следующая задача после утверждения должна отдельно перечислить production
  whitelist для mobile Mafia и TV Mafia.

---

## Что НЕ сделано

- Новые макеты не подключены к `src/app/game/[roomId]/mafia/page.tsx`.
- TV-рендер Мафии не менялся.
- Варианты ночных действий для каждой расширенной роли пока отражены общей
  системой выбора цели, а не отдельными полноразмерными макетами.

---

## Подсказки для ревью

- Новая интерактивная секция начинается с `ClubPhaseId` и компонента
  `ClubStorySection` в `src/app/mafia-design-preview/page.tsx`.
- Макеты используют один визуальный словарь: тёмный фиолетовый бархат, латунь,
  винно-красный danger-акцент и Georgia для церемониальных заголовков.
- Переключение фаз — только локальный state preview-страницы; socket/game state
  не используется.
