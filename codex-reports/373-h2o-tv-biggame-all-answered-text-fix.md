# REPORT TASK-373: TV «100 к 1» Большая игра — текст «все вопросы отвечены»

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-20 22:47
> - **Финиш:** 2026-07-20 22:49
> - **Длительность:** 2 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Исправлен текст TV-сообщения в Большой игре «100 к 1», когда текущий игрок ответил на все 5 вопросов. Сообщение теперь грамматически корректное и включает имя игрока P1/P2 в зависимости от `bgPhase`.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — заменён текст в ветке `(h.bgPhase === 1 || h.bgPhase === 3) && h.bgCurQ >= 5`; условие, стили и else-ветка со списком вопросов не менялись.

### Новые файлы

- `codex-reports/373-h2o-tv-biggame-all-answered-text-fix.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

Полный `git diff --stat -- src/app/tv/[roomId]/[gameType]/page.tsx` сейчас включает незакоммиченные изменения предыдущих задач, которые уже были в рабочем дереве до старта TASK-373:

```text
src/app/tv/[roomId]/[gameType]/page.tsx | 675 ++++++++++++++++++++++++--------
1 file changed, 503 insertions(+), 172 deletions(-)
```

Фактический diff TASK-373 по строкам:

```diff
- {l('Все вопросы отвечены — ждём ведущего…', 'All questions answered — waiting for the host…')}
+ {l(
+   `Игрок ${h2oPlayerName(h.bgPhase === 1 ? h.bgP1Id : h.bgP2Id)} ответил на все вопросы — ждём проверки ведущего`,
+   `${h2oPlayerName(h.bgPhase === 1 ? h.bgP1Id : h.bgP2Id)} answered all questions — waiting for the host to check`
+ )}
```

Текущие строки: `src/app/tv/[roomId]/[gameType]/page.tsx:1619-1622`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npx tsc --noEmit` | ✅ | без ошибок |
| `npm run lint` | ✅ | без warnings/errors |
| Acceptance: русский текст грамматически корректен и включает имя | ✅ | `Игрок ${h2oPlayerName(...)} ответил на все вопросы — ждём проверки ведущего` |
| Acceptance: условие показа не изменено | ✅ | осталось `(h.bgPhase === 1 || h.bgPhase === 3) && h.bgCurQ >= 5` |
| Acceptance: остальной рендер блока не затронут | ✅ | менялся только вызов `l(...)` внутри существующего `<div>` |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверить `src/app/tv/[roomId]/[gameType]/page.tsx:1619` — единственная смысловая правка TASK-373.
