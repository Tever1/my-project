# REPORT TASK-005.3: Убрать ахроматический проход из strip-bg.mjs

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-03 17:00
> - **Финиш:** 2026-05-03 17:02
> - **Длительность:** 2 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Из `scripts/strip-bg.mjs` удалён ахроматический проход: константы, функция и вызов `stripBackgroundPixels`. Единственный механизм удаления фона теперь `floodFillFromCorners`; `FLOOD_LIGHT_THRESHOLD = 180` не менялся.

---

## Что сделано

### Изменённые файлы

- `scripts/strip-bg.mjs` — удалены `LIGHT_THRESHOLD`, `CHROMA_THRESHOLD`, `stripBackgroundPixels()` и её вызов; лог теперь выводит один счётчик flood-fill pixels stripped.

### Новые файлы

- `codex-reports/005.3-strip-bg-drop-achromatic.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

`git diff --stat` показывает только tracked-файлы и сейчас содержит pre-existing изменения вне этого таска:

```text
 .codex/STATUS.md                      |   7 +-
 package-lock.json                     |   7 +-
 package.json                          |   4 +-
 public/icons/games/mafia.png          | Bin 1736803 -> 2764210 bytes
 public/icons/games/quiz.png           | Bin 1234034 -> 1820948 bytes
 src/app/admin/page.tsx                |   2 +-
 src/app/api/admin/game-stats/route.ts |   2 +-
 src/app/design-tokens/page.tsx        | 134 +++++++++++++++++++++++++++++++++-
 src/app/lobby-preview/page.tsx        |   2 +-
 9 files changed, 149 insertions(+), 9 deletions(-)
```

`scripts/strip-bg.mjs` находится в worktree как untracked-файл, поэтому не попадает в обычный `git diff --stat`:

```text
?? scripts/strip-bg.mjs
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `node --check scripts/strip-bg.mjs` | ✅ | Синтаксис OK |
| no `LIGHT_THRESHOLD` / `CHROMA_THRESHOLD` / `stripBackgroundPixels` | ✅ | Точная `grep`-проверка запрещённых идентификаторов без совпадений |
| `npm run lint` | ❌ | Падает на существующих ошибках вне whitelist: `.agents/skills/*`, `mobile/*`, `src/app/admin/page.tsx`, `src/app/game/[roomId]/quiz/page.tsx`, `src/lib/use-socket.ts` и др.; новых ошибок в `scripts/strip-bg.mjs` не показано |
| `npm run build` | ❌ | Turbopack упал из-за sandbox: `creating new process`, `binding to a port`, `Operation not permitted` при обработке `node_modules/geist/dist/geistsans_d5a4f12f.module.css` |

---

## Отклонения от ТЗ

- `git pull` перед стартом не прошёл: `error: cannot open '.git/FETCH_HEAD': Operation not permitted`. Продолжил после проверки `.codex/STATUS.md`; коллизий по whitelist нет.
- `npm run strip-bg` не запускался, как и просил пользователь.
- Коммит не делался.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Все изменения по ТЗ сделаны. Линт и build не подтверждены из-за внешних/pre-existing ошибок и sandbox-ограничения, описанных выше.

---

## Подсказки для ревью

- Проверь `scripts/strip-bg.mjs`: после правки поток обработки такой: `ensureAlpha().raw()` → `floodFillFromCorners(data, width, height)` → запись PNG.
- `FLOOD_LIGHT_THRESHOLD = 180` оставлен без изменений.
