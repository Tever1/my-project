# REPORT TASK-419: предупреждение Next.js о плавной прокрутке

> **Статус:** ✅ done

## Резюме

На корневой элемент `<html>` добавлен атрибут `data-scroll-behavior="smooth"`. Он подтверждает Next.js, что глобальный `scroll-behavior: smooth` используется намеренно, и устраняет соответствующее предупреждение в консоли без изменения поведения интерфейса.

## Изменённые файлы

- `src/app/layout.tsx` — добавлен штатный атрибут Next.js.

## Проверки

- `npx tsc --noEmit` — пройдено.
- ESLint — пройдено.
- `git diff --check` — пройдено.
- Браузерный QA не запускался по правилу пользователя.
