# REPORT TASK-417: размер диапазона игроков в выборе режима

> **Статус:** ✅ done

## Резюме

Диапазоны `4–10 игроков` и `2–10 игроков` теперь имеют тот же размер шрифта, что и названия режимов. Изменение синхронизировано в production-интерфейсе и preview-макете; игровая логика не затрагивалась.

## Изменённые файлы

- `src/app/globals.css` — размер диапазона увеличен с `8px` до `17px`.
- `src/app/alias-design-preview/page.tsx` — размер диапазона увеличен с `7px` до `15px`, как у названий в макете.

## Проверки

- `git diff --check` — пройдено.
- Браузерный QA не запускался по правилу пользователя.
