# REPORT TASK-399: Шпион — интеграция «Оперативного центра»

> **Метаданные**
> - **Старт:** 2026-08-10 20:40 PDT
> - **Финиш:** 2026-08-10 22:12 PDT
> - **Длительность:** 92 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Утверждённый дизайн «Оперативный центр» перенесён из
`/spy-design-preview#operations-full-flow` в production-экраны телефона и TV.
Старая универсальная компоновка заменена отдельными экранами всех игровых фаз;
существующая socket-логика и правила игры сохранены.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/spy/page.tsx` — новая production-компоновка телефона:
  выбор режима, защищённое раскрытие роли удерживанием, допрос, общий холст,
  обсуждение, тайное голосование, попытка шпиона, ручная проверка, итог раунда
  и завершение операции.
- `src/app/tv/[roomId]/[gameType]/page.tsx` — новый TV «Оперативный центр» со
  специальными экранами каждой фазы, приватным статусом голосов и отдельным
  экраном ожидания решения проверяющего.
- `src/app/globals.css` — адаптивная визуальная система production-экранов:
  радарная сетка, сигнал/опасность, карточки агентов, таймеры, холст, TV-сетка,
  focus-состояния и `prefers-reduced-motion`.

### Новые файлы

- `src/lib/guest-player-id.ts` — ранее начатая в рамках того же незавершённого
  прогона централизация независимой guest-сессии для отдельных телефонов.

### Удалённые файлы

- (нет)

---

## Diff stat

```text
src/app/game/[roomId]/spy/page.tsx      | 663 +++++++++-------------------
src/app/globals.css                     | 400 +++++++++++++++++
src/app/tv/[roomId]/[gameType]/page.tsx | 736 ++++++++++++++++----------------
3 files changed, 964 insertions(+), 835 deletions(-)
```

`src/app/tv/[roomId]/[gameType]/page.tsx` уже содержал незакоммиченные правки
Мафии из предыдущих задач, поэтому stat общего файла включает и их. Они не
переписывались в TASK-399.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | 0 ошибок; 4 прежних warning в незатронутом файле Мафии |
| Scoped ESLint | ✅ | `spy/page.tsx` + общий TV-файл |
| `npx tsc --noEmit` | ✅ | ошибок нет |
| `git diff --check` | ✅ | ошибок форматирования diff нет |
| Production phone route | ✅ | `/game/765EZ5/spy`, DOM + screenshot |
| Production TV route | ✅ | `/tv/765EZ5/spy`, DOM + screenshot |
| Утверждённые отдельные фазы | ✅ | mobile + TV markup реализован для всех состояний |

Production build не запускался при работающем dev-сервере, чтобы не повторить
конфликт `.next` между `next build` и dev-режимом и не прервать живую комнату.

---

## Отклонения от ТЗ

Нет. Макеты перенесены в production без изменения правил игры. Английские
строки добавлены парно с русскими согласно правилам проекта.

---

## Открытые вопросы для Claude

Нет.

---

## Подсказки для ревью

- Проверить фазу `spyGuess`: TV специально различает обычную попытку шпиона и
  состояние `spyGuessAwaitingJudge`, не раскрывая правильное слово общему экрану.
- В голосовании TV показывает только факт принятия голоса, но не выбранную цель.
- Раздача и peek-bar скрывают секрет сразу после отпускания/потери фокуса.
