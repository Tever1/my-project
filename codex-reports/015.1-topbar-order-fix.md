# REPORT TASK-015.1: Visual left-to-right keyboard order в TopBar

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-03 23:22
> - **Финиш:** 2026-05-03 23:24
> - **Длительность:** 2 минуты
> - **Статус:** ⚠️ partial

---

## Резюме (TL;DR)

Кодовое изменение выполнено по ТЗ: TopBar order теперь идёт слева направо
`play → friends-nav → history → friends-online → room`. Проверки запущены,
но `lint` падает на уже существующих ошибках вне whitelist, а `build` падает
из-за sandbox/Turbopack restriction при создании процесса.

---

## Что сделано

### Изменённые файлы

- `src/app/lobby-preview/page.tsx` — добавлен `topbarId="friends-nav"` для nav-кнопки «Друзья», `FriendsOnlinePill` переведён на `topbarId="friends-online"`, order array обновлён до 5 элементов.

### Новые файлы

- `codex-reports/015.1-topbar-order-fix.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/lobby-preview/page.tsx | 29 +++++++++++++++++++++++------
 1 file changed, 23 insertions(+), 6 deletions(-)
```

Примечание: stat включает незакоммиченные изменения из предыдущего TASK-015
в этом же файле. В рамках TASK-015.1 внесены только 3 точки из ТЗ.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ❌ | Sandbox: `error: cannot open '.git/FETCH_HEAD': Operation not permitted` |
| `npm run lint` | ❌ | 73 существующие проблемы вне whitelist (`.agents/skills`, `mobile/*`, `src/app/game/*`, `src/lib/*`) |
| `npm run build` | ❌ | Turbopack internal error: `creating new process - binding to a port - Operation not permitted` |
| Acceptance #1: `npm run build` ОК | ❌ | Заблокировано sandbox/Turbopack restriction |
| Acceptance #2: → order | ✅ | По коду: `["play", "friends-nav", "history", "friends-online", "room"]` |
| Acceptance #3: ← reverse order | ✅ | Тот же order array используется с `direction = -1` |
| Acceptance #4: no wrap on edges | ✅ | Логика `if (next < 0 || next >= order.length) return` сохранена |
| Acceptance #5: focus ring на 5 элементах | ✅ | Все 5 элементов являются button с `data-topbar`; TASK-015 уже добавил focus ring для FriendsOnlinePill |

---

## Отклонения от ТЗ

Кодовых отклонений нет. Проверки `git pull`, `lint` и `build` не удалось
получить зелёными из-за ограничений окружения и существующих ошибок вне whitelist.

---

## Открытые вопросы для Claude

нет

---

## Что НЕ сделано (если статус ⚠️ или ❌)

- Не подтверждён зелёный `npm run build`: Turbopack упал на sandbox restriction
  `Operation not permitted`.
- Не исправлялись lint-ошибки вне whitelist.

---

## Подсказки для ревью

- Проверь `src/app/lobby-preview/page.tsx:170`, `:371`, `:379` — это три точки изменения TASK-015.1.
