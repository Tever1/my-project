# REPORT TASK-372: TV «100 к 1» Большая игра — сообщение, когда все 5 вопросов отвечены

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-20 22:44
> - **Финиш:** 2026-07-20 22:45
> - **Длительность:** 1 минута
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Добавлена fallback-ветка для TV Большой игры: когда игрок ответил на все 5 вопросов и `bgCurQ >= 5`, левая колонка больше не пустеет, а показывает сообщение ожидания ведущего. Рендер карточек вопросов не менялся, он только обёрнут в условие.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — в блоке TV `hundred-to-one` для `bgPhase === 1 || bgPhase === 3` добавлено сообщение `Все вопросы отвечены — ждём ведущего…` при `h.bgCurQ >= 5`; существующий `activeBigQ.map(...)` оставлен в альтернативной ветке без изменения внутренней карточки.

### Новые файлы

- `codex-reports/372-h2o-tv-biggame-all-answered-message.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
src/app/tv/[roomId]/[gameType]/page.tsx | 672 ++++++++++++++++++++++++--------
1 file changed, 500 insertions(+), 172 deletions(-)
```

Примечание: stat выше включает уже существующие незакоммиченные изменения в TV-файле до TASK-372. Diff TASK-372 по строкам: `src/app/tv/[roomId]/[gameType]/page.tsx:1617` добавляет условие `h.bgCurQ >= 5`, строки `1618-1620` добавляют сообщение, строки `1621-1643` оставляют прежний `.map()` в `else`-ветке.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npx tsc --noEmit` | ✅ | без вывода, exit 0 |
| `npm run lint` | ✅ | `eslint`, exit 0 |
| Acceptance: `bgCurQ < 5` | ✅ | прежний `.map()` и фильтр текущего вопроса сохранены |
| Acceptance: `bgCurQ >= 5` на `bgPhase === 1 || 3` | ✅ | показывается сообщение ожидания ведущего |
| Acceptance: `bgPhase === 2 || 4` | ✅ | условие не срабатывает, используется прежний список вопросов для проверки |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Смотреть точечный участок `src/app/tv/[roomId]/[gameType]/page.tsx:1617`: добавлена только альтернативная ветка перед существующим рендером `activeBigQ.map(...)`.
