# REPORT TASK-374: Мобильный экран `/join/[code]` — кнопка «Вернуться в лобби»

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-20 22:58
> - **Финиш:** 2026-07-20 22:59
> - **Длительность:** 1 минута
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

На мобильной странице `/join/[code]` добавлено локальное состояние для кнопки добавления игрока. После клика по «+ Добавить игрока» кнопка заменяется на «Вернуться в лобби», а повторный клик эмитит тот же `room:show-qr` toggle и возвращает исходную кнопку.

---

## Что сделано

### Изменённые файлы

- `src/app/join/[code]/page.tsx` — добавлен перевод `backToLobby`, состояние `qrShown`, обработчик `handleCloseAddPlayerQr`, и условный рендер кнопки на месте старой «+ Добавить игрока».

### Новые файлы

- `codex-reports/374-join-page-add-player-return-to-lobby-button.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff по строкам

- `src/app/join/[code]/page.tsx:44` — добавлен перевод `backToLobby`.
- `src/app/join/[code]/page.tsx:79` — добавлено состояние `qrShown`.
- `src/app/join/[code]/page.tsx:209-219` — `handleAddPlayer` теперь выставляет `qrShown=true`, добавлен `handleCloseAddPlayerQr` с повторным emit и `qrShown=false`.
- `src/app/join/[code]/page.tsx:570-608` — старая кнопка заменена на условный рендер с теми же inline-стилями.

---

## Diff stat

```
 src/app/join/[code]/page.tsx | 66 ++++++++++++++++++++++++++++++++------------
 1 file changed, 48 insertions(+), 18 deletions(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npx tsc --noEmit` | ✅ | без ошибок |
| `npm run lint` | ✅ | без warnings/errors |
| До клика видна «+ Добавить игрока» | ✅ | сохранён исходный `qrShown=false` |
| После клика появляется «Вернуться в лобби» | ✅ | `handleAddPlayer` ставит `qrShown=true` |
| Повторный клик эмитит `room:show-qr` и возвращает кнопку | ✅ | `handleCloseAddPlayerQr` ставит `qrShown=false` |

---

## Отклонения от ТЗ

нет отклонений.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

—

---

## Подсказки для ревью

- Проверить, что дублированный inline-style у двух веток кнопки совпадает с исходным стилем.
