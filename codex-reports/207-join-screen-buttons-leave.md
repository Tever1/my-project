# REPORT TASK-207: Экран комнаты на телефоне — кнопки старта, добавления игрока и выхода

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-06-03 21:35
> - **Финиш:** 2026-06-03 21:40
> - **Длительность:** 5 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В `src/app/join/[code]/page.tsx` добавлена логика выхода из комнаты с подтверждением, постоянные кнопки `+ Добавить игрока` и `Выйти`, а `НАЧАТЬ ИГРУ` теперь показывается только при выбранной игре. Проверки `npx tsc --noEmit` и `npm run lint` прошли без ошибок.

---

## Что сделано

### Изменённые файлы

- `src/app/join/[code]/page.tsx` — добавлены `useRouter`, состояние `confirmLeave`, `handleLeaveRoom`, перестроен блок действий в joined-состоянии и добавлена модалка подтверждения выхода.

### Новые файлы

- `codex-reports/207-join-screen-buttons-leave.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/join/[code]/page.tsx | 358 +++++++++++++++++++++++++++++++++++++------
 1 file changed, 310 insertions(+), 48 deletions(-)
```

Примечание: в целевом файле уже были незакоммиченные изменения до TASK-207, поэтому stat отражает общий diff файла относительно HEAD, а не только добавления этой задачи.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npx tsc --noEmit` | ✅ | 0 ошибок |
| `npm run lint` | ✅ | без ошибок |
| Acceptance: хост без выбранной игры | ✅ | `НАЧАТЬ ИГРУ` не рендерится, показывается подсказка + постоянные кнопки |
| Acceptance: хост с выбранной игрой | ✅ | `НАЧАТЬ ИГРУ` рендерится при `canStartGame && roomState?.currentGame` |
| Acceptance: не-хост | ✅ | показывается `Ожидание ведущего...` + постоянные кнопки |
| Acceptance: выход | ✅ | модалка подтверждения; `Выйти` вызывает `room:leave` и `router.push("/")`; отмена/backdrop закрывают |

---

## Отклонения от ТЗ

Нет отклонений по реализации. Отчёт создан вне whitelist, потому что он явно требуется в секции "Отчёт".

---

## Открытые вопросы для Claude

В рабочем дереве до начала работы уже были изменения в файлах вне whitelist, включая `CLAUDE.md`, `.codex/STATUS.md`, `codex-tasks/**` и другие. Я их не редактировал и не откатывал.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверь блок действий в `src/app/join/[code]/page.tsx`: `НАЧАТЬ ИГРУ` завязан на `canStartGame && roomState?.currentGame`, а `+ Добавить игрока` и `Выйти` находятся вне этой развилки.
- Проверь `handleLeaveRoom`: он следует ТЗ и эмитит `room:leave` без payload.
