# REPORT TASK-178: Quiz мобилка — убрать статус-текст, убрать scoreboard-бар, унифицировать спец-кнопку

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-30 20:58
> - **Финиш:** 2026-05-30 21:04
> - **Длительность:** 6 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Мобильная страница квиза очищена по ТЗ: убран статус-текст под ответами, отключен scoreboard-бар в `GameLayout`, кнопка выбора спец-квиза приведена к обычному amber-стилю setup-кнопок. Проверки `npm run lint` и `npx tsc --noEmit` прошли.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/quiz/page.tsx` — `showScoreboard` принудительно выключен для мобильной страницы квиза; удален блок Status bar под вариантами ответов; стиль кнопки спец-квиза заменен на обычный amber setup-style.

### Новые файлы

- `codex-reports/178-quiz-mobile-cleanup.md` — отчет по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

Task-only diff:

```text
 src/app/game/[roomId]/quiz/page.tsx | 15 ++-------------
 1 file changed, 2 insertions(+), 13 deletions(-)
```

Примечание: в рабочей копии до старта уже были изменения в `.codex/STATUS.md` и `CLAUDE.md`, а также untracked отчеты/таски 171-177. Я их не трогал.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| Acceptance #1 | ✅ | Блок «Ответ принят» / «Выберите ответ» удален |
| Acceptance #2 | ✅ | `showScoreboard={false}` в мобильном quiz page |
| Acceptance #3 | ✅ | Кнопка спец-квиза использует `border` и amber `600/20` → `500/5` |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверь `src/app/game/[roomId]/quiz/page.tsx`: изменения намеренно ограничены UI мобильной страницы квиза; TV-страница и `GameLayout` не менялись.
