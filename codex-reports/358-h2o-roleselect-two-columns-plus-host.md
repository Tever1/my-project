# REPORT TASK-358: «100 к 1» TV — список неопределившихся в 2 колонки + статус ведущего снизу

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-15 22:55
> - **Финиш:** 2026-07-15 23:05
> - **Длительность:** 10 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В TV-блоке `hundred-to-one` для `roleSelect` список игроков без роли разбит на две колонки через половинное деление массива. Внизу центральной карточки добавлен статус ведущего с плейсхолдером `не выбран / not chosen`, если ведущий ещё не выбран.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — в `h.phase === 'roleSelect'` заменён вертикальный список `h2oUnassignedPlayers` на две колонки; добавлена нижняя строка статуса ведущего; исправлены tab-отступы в конце тернарника.

### Новые файлы

- `codex-reports/358-h2o-roleselect-two-columns-plus-host.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
src/app/tv/[roomId]/[gameType]/page.tsx | 643 +++++++++++++++++++++++---------
1 file changed, 469 insertions(+), 174 deletions(-)
```

Примечание: stat по файлу включает уже существующие незакоммиченные изменения предыдущих задач в этом же файле. Моя правка ограничена JSX-блоком `roleSelect`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | чисто |
| `npx tsc --noEmit` | ✅ | чисто |
| Acceptance: 2 колонки в `roleSelect` | ✅ | массив делится через `Math.ceil(length / 2)` |
| Acceptance: статус ведущего снизу | ✅ | показывает `h2oHost.nickname` или `l('не выбран', 'not chosen')` |

---

## Отклонения от ТЗ

Нет отклонений по production-коду. QR-ветка для `captainSelect`/`teamNames` не менялась функционально; исправлены только отступы в конце тернарника.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Посмотреть только `src/app/tv/[roomId]/[gameType]/page.tsx` в районе JSX `h.phase === 'roleSelect'`.
- В рабочем дереве до моей правки уже были изменения в `.codex/STATUS.md` и других файлах; я их не откатывал и не редактировал.
