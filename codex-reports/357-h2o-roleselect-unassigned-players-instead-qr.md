# REPORT TASK-357: «100 к 1» TV — roleSelect показывает игроков без роли вместо QR

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-15 22:40
> - **Финиш:** 2026-07-15 22:51
> - **Длительность:** 11 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В TV-блоке `hundred-to-one` центральная карточка на фазе `roleSelect` теперь показывает игроков, которые ещё не выбрали роль. Для `captainSelect` и `teamNames` прежний QR-блок сохранён без изменения поведения.

---

## Что сделано

### Изменённые файлы

- `src/app/tv/[roomId]/[gameType]/page.tsx` — добавлен список `h2oUnassignedPlayers`; центральная карточка prep-грида условно рендерит список игроков без роли только для `roleSelect`, с fallback «Все выбрали роль ✓» и прокруткой длинного списка.

### Новые файлы

- `codex-reports/357-h2o-roleselect-unassigned-players-instead-qr.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
src/app/tv/[roomId]/[gameType]/page.tsx | 626 +++++++++++++++++++++++---------
1 file changed, 453 insertions(+), 173 deletions(-)
```

Примечание: рабочее дерево уже содержало незакоммиченные изменения в этом файле до TASK-357, поэтому stat выше отражает накопительный diff файла. Локальная правка TASK-357 ограничена вычислением `h2oUnassignedPlayers` и условным содержимым центральной карточки в prep-блоке.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| Acceptance #1 | ✅ | lint и tsc чисто |
| Acceptance #2 | ✅ | `roleSelect` показывает игроков без роли с аватаром и никнеймом |
| Acceptance #3 | ✅ | `captainSelect`/`teamNames` оставлены на прежнем QR-блоке |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Смотреть условный рендер центральной карточки в `src/app/tv/[roomId]/[gameType]/page.tsx` внутри блока `h.phase === 'roleSelect' || h.phase === 'captainSelect' || h.phase === 'teamNames'`.
- Обратить внимание, что QR JSX сохранён в ветке `h.phase !== 'roleSelect'`.
