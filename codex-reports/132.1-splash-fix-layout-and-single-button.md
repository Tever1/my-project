# REPORT TASK-132.1: Splash — фикс layout + одна кнопка по auto-detect

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-24 18:40
> - **Финиш:** 2026-05-24 18:58
> - **Длительность:** 18 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Splash переписан на одну auto-detected кнопку без `GlassButton`, чтобы убрать `nowrap`/кликабельный layout-баг. `npm run lint` и `npx tsc --noEmit` прошли чисто; браузерная проверка и `npm run build` заблокированы sandbox-ограничением на `listen`/spawn.

---

## Что сделано

### Изменённые файлы

- `src/components/Splash.tsx` — удалён dual-button flow, `GlassButton` заменён на один `motion.button`; режим выбирается через `useIsMobile()` (`desktop` → «Играть», `mobile` → «Играть на телефоне»).

### Новые файлы

- `codex-reports/132.1-splash-fix-layout-and-single-button.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
/dev/null => src/components/Splash.tsx | 70 ++++++++++++++++++++++++++++++++++
1 file changed, 70 insertions(+)
```

Примечание: `src/components/Splash.tsx` уже был untracked после TASK-132, поэтому обычный `git diff --stat` не показывает его содержимое; stat выше получен через `git diff --no-index --stat /dev/null src/components/Splash.tsx`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ✅ | Already up to date |
| `npm run lint` | ✅ | Без ошибок |
| `npx tsc --noEmit` | ✅ | Без ошибок |
| `npm run build` | ⚠️ | Turbopack упал из-за sandbox: `creating new process` / `binding to a port` / `Operation not permitted` |
| Browser desktop `http://localhost:3000` | ⚠️ | Dev server не стартует в sandbox: `listen EPERM 127.0.0.1:3000` |
| Browser mobile viewport | ⚠️ | Заблокировано той же причиной |
| Acceptance: одна кнопка desktop/mobile | ✅ | Реализовано через `targetMode: PlayMode = isMobile ? "mobile" : "desktop"` |
| Acceptance: текст не вылезает | ✅ | Убраны `GlassButton`/`nowrap`; кнопка `flex flex-col`, `w-full max-w-md`, `min-h-[180px]`, текст переносится |
| Acceptance: click по тексту/кнопке | ✅ | `onClick` висит на самом `motion.button`, текст внутри кнопки |
| Acceptance: localStorage mode | ✅ | Используется существующий `setMode(targetMode)` |

---

## Отклонения от ТЗ

Нет отклонений в коде. Визуальную проверку через браузер выполнить не удалось из-за ограничений текущего sandbox на открытие портов.

---

## Открытые вопросы для Claude

нет

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Браузерная QA не выполнена локально: `npm run dev`, `TMPDIR=/private/tmp npm run dev`, `npx next dev -p 3000` и `npx next dev -H 127.0.0.1 -p 3000` падают с `EPERM` на `listen`.

---

## Подсказки для ревью

- Посмотреть `src/components/Splash.tsx`: важно, что `GlassButton` больше не импортируется и не используется.
- Проверить в реальном браузере 375px / 768px / 1024px / 1440px: в этой sandbox-сессии порт открыть нельзя.
