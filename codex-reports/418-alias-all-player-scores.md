# REPORT TASK-418: все игроки в строке счёта Alias

> **Статус:** ✅ done

## Резюме

В режиме «На букву» нижняя строка счёта больше не обрезается до двух игроков. В ней присутствуют все участники; при длинном составе строку можно прокрутить горизонтально без визуальной полосы прокрутки.

## Изменённые файлы

- `src/app/game/[roomId]/alias/page.tsx` — для режима «На букву» удалено ограничение таблицы двумя игроками; классический режим по-прежнему показывает две команды.
- `src/app/globals.css` — строка счёта получила горизонтальную прокрутку вместо многоточия.

## Проверки

- `npx tsc --noEmit` — пройдено.
- ESLint production-файла — пройдено.
- `git diff --check` — пройдено.
- Браузерный QA не запускался по правилу пользователя.

## Пункт 3 из TASK-415

Предлагаемое исправление предупреждения Next.js: добавить `data-scroll-behavior="smooth"` на корневой элемент `<html>` в layout, где задана плавная прокрутка. В рамках TASK-418 эта отдельная правка не вносилась.
