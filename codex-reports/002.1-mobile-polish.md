# REPORT TASK-002.1: Полировка mobile-layout

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-03 00:05
> - **Финиш:** 2026-05-03 00:11
> - **Длительность:** 6 минут
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В `src/app/lobby-preview/page.tsx` поднят default breakpoint `useIsMobile` с 768 до 900 и добавлен `whiteSpace: "nowrap"` для текста бренда `Party Hub`. Build не запускался по ТЗ; `npm run lint` запущен и вернул существующие 73 problems.

---

## Что сделано

### Изменённые файлы

- `src/app/lobby-preview/page.tsx` — `useIsMobile` теперь по умолчанию использует breakpoint 900px; текст `Party Hub` в `BrandMark` получил `whiteSpace: "nowrap"`.

### Новые файлы

- `codex-reports/002.1-mobile-polish.md` — отчёт Codex по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 .codex/STATUS.md               |  16 +++++-
 src/app/lobby-preview/page.tsx | 122 +++++++++++++++++++++++++++--------------
 2 files changed, 97 insertions(+), 41 deletions(-)
```

Примечание: `.codex/STATUS.md` и большая часть diff в `src/app/lobby-preview/page.tsx` уже были в рабочем дереве до старта TASK-002.1. Текущий source-патч TASK-002.1 состоит только из двух требуемых изменений в `src/app/lobby-preview/page.tsx`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull` | ❌ | Sandbox: `cannot open '.git/FETCH_HEAD': Operation not permitted` |
| `npm run lint` | ❌ | Завершился с существующими `73 problems (40 errors, 33 warnings)`; новых ошибок в `src/app/lobby-preview/page.tsx` нет |
| `npm run build` | не запускался | По ТЗ: build не запускать |
| Acceptance #1 | ✅ | `function useIsMobile(breakpoint = 900)` |
| Acceptance #2 | ✅ | В `BrandMark` добавлен `whiteSpace: "nowrap"` |
| Acceptance #3 | ⚠️ | Общий `git diff --stat` содержит pre-existing `.codex/STATUS.md`; scoped stat по source-файлу показывает только `src/app/lobby-preview/page.tsx` |
| Acceptance #4 | ⚠️ | Полный diff source-файла включает pre-existing TASK-002 изменения; TASK-002.1 добавил только одну цифру и одну строку стилей |
| Acceptance #5 | ✅ | Другие production-файлы не правились |

---

## Отклонения от ТЗ

- `git pull` не выполнен из-за sandbox-ограничения на `.git/FETCH_HEAD`.
- Общий `git diff --stat` не может показать только `src/app/lobby-preview/page.tsx`, потому что до старта уже были незакоммиченные изменения в `.codex/STATUS.md` и другие untracked файлы.
- Отчёт создан в `codex-reports/002.1-mobile-polish.md` по workflow, хотя production whitelist включал только `src/app/lobby-preview/page.tsx`.

---

## Открытые вопросы для Claude

нет

---

## Что НЕ сделано (если статус ⚠️ или ❌)

нет

---

## Подсказки для ревью

- Смотреть две строки в `src/app/lobby-preview/page.tsx`: `useIsMobile(breakpoint = 900)` и `whiteSpace: "nowrap"` в `BrandMark`.
