# TASK-532.1 — Missing word list recovery

2026-09-14. Follow-up to TASK-532 after the owner reported invisible lists.

Read-only catalog inspection returned Alias 80, Crocodile 50 and Spy 93 entries.
WordStudio previously rendered an empty array silently when the retained browser
workspace draft did not include the newly added words field. This is a confirmed
code path, not a confirmed reproduction of the owner's browser state.

Added a one-time refresh for missing word sections, a visible reload-list button
and workspace/error feedback. Existing drafts, pending edits and game banks are
preserved. File changed: src/components/admin/ContentStudio.tsx.

Current baseline themed backgrounds: harry-potter.webp and marvel.webp.
Catalog validation accepts PNG, WebP, JPG and JPEG. PNG-only filtering applies
only to the unassigned background gallery, not the runtime quiz background.

TypeScript, scoped ESLint and diff-check passed. No browser QA, model call,
server restart, build, commit or push. Owner should reload admin and select the
game; if lists remain invisible, actual browser evidence is required rather than
assuming the stale-workspace path was the only cause.
