# REPORT TASK-391: Ведущий в таблице распределения ролей

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-08-06
> - **Финиш:** 2026-08-06 22:26 PDT
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В админскую таблицу распределения ролей Мафии добавлена колонка «Ведущий».
Количество людей в каждой строке увеличено на один, поэтому минимальный состав
теперь показан как 5 человек: ведущий, Мафия и три Мирных. Игровая раздача и
макеты Мафии не менялись.

---

## Что сделано

### Изменённые файлы

- `src/app/admin/page.tsx` — добавлены тип поля `host`, заголовок и ячейки
  «Ведущий»; минимальная ширина таблицы увеличена для новой колонки.
- `src/app/api/admin/game-data/route.ts` — во всех строках добавлен один ведущий,
  общий счётчик участников изменён с диапазона 4–16 на 5–17; подписи условий
  появления ролей теперь тоже считают ведущего.
- `codex-reports/CODEX-HANDOFF.md` — следующая задача переключена на TASK-392.

### Новые файлы

- `codex-reports/391-admin-mafia-host-column.md` — этот отчёт.

### Удалённые файлы

- Нет.

---

## Diff stat

```text
 src/app/admin/page.tsx               |  6 ++++--
 src/app/api/admin/game-data/route.ts | 36 ++++++++++++++++++------------------
 2 files changed, 22 insertions(+), 20 deletions(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `git pull --ff-only` | ✅ | Ветка актуальна |
| `npx tsc --noEmit` | ✅ | Ошибок нет |
| `npm run lint` | ✅ | 0 ошибок; 4 прежних warning в Mafia page |
| `git diff --check` | ✅ | Ошибок нет |
| Программная сверка таблицы | ✅ | 13 строк, диапазон 5–17, ведущий всегда 1, сумма колонок совпадает с общим числом людей |
| Browser QA `/admin` | ✅ | Колонка видна; первая строка `5 / 1 / 1 / … / 3`; обновлённые условия ролей отображаются |
| `npm run build` | ⚠️ | Снова завис без ошибки на стадии оптимизации; остановлен вручную |

---

## Отклонения от ТЗ

Нет.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано

Игровые пороги `assignRoles()` намеренно не менялись: там по-прежнему считается
только число игроков с ролями, без ведущего.

---

## Подсказки для ревью

- В API поле `players` теперь означает общее число присутствующих вместе с
  ведущим; сумма `host + mafia + don + maniac + sheriff + doctor + lover +
  citizens` равна `players` в каждой строке.
