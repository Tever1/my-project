# REPORT TASK-318: Alias "Название команды" — сабмит по Enter

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-07-01 00:43
> - **Финиш:** 2026-07-01 00:45
> - **Длительность:** 2 минуты
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

В `TeamNameInput` добавлен `onKeyDown` для сабмита по Enter при непустом значении. Логика совпадает с кнопкой "Готово": вызывается `onSubmit(value)`.

---

## Что сделано

### Изменённые файлы

- `src/app/game/[roomId]/alias/page.tsx` — в поле ввода названия команды добавлен обработчик Enter.

### Новые файлы

- `codex-reports/318-alias-teamname-enter-submit.md` — отчёт по задаче.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/app/game/[roomId]/alias/page.tsx | 38 ++++++++++++++++++++++++++----------
 1 file changed, 28 insertions(+), 10 deletions(-)
```

Примечание: до старта TASK-318 в `src/app/game/[roomId]/alias/page.tsx` уже были незакоммиченные изменения от предыдущих задач. Изменение TASK-318 — только добавление `onKeyDown` в `TeamNameInput`.

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | — |
| `npx tsc --noEmit` | ✅ | — |
| Acceptance: Enter с непустым значением вызывает сабмит | ✅ | Добавлен `onKeyDown` с проверкой `e.key === 'Enter' && value.trim()` |

---

## Отклонения от ТЗ

Нет отклонений.

---

## Открытые вопросы для Claude

Нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

Не применимо.

---

## Подсказки для ревью

- Проверить `src/app/game/[roomId]/alias/page.tsx:96` — добавлен единственный обработчик Enter для `TeamNameInput`.
