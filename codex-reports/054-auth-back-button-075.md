# REPORT TASK-054: Auth back button 0.75

> **Метаданные** (заполняет Codex)
> - **Старт:** 2026-05-10 13:31 PDT
> - **Финиш:** 2026-05-10 13:32 PDT
> - **Длительность:** 1 минута
> - **Статус:** ✅ done

---

## Резюме (TL;DR)

Secondary-фон кнопки «Назад» в auth modal изменён с `rgba(255,255,255,0.20)` на `rgba(255,255,255,0.75)`. Primary-ветка и остальные стили не менялись.

---

## Что сделано

### Изменённые файлы

- `src/components/lobby/Lobby.tsx` — в `btnStyle` обновлена secondary-ветка `background`: `0.20 → 0.75`.

### Новые файлы

- `codex-reports/054-auth-back-button-075.md` — отчёт по TASK-054.

### Удалённые файлы

- (нет)

---

## Diff stat

```
 src/components/lobby/Lobby.tsx | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

---

## Проверки

| Проверка | Результат | Заметки |
|---|---|---|
| `npm run lint` | ✅ | без ошибок |
| `npx tsc --noEmit` | ✅ | без ошибок |
| Diff только одна строка | ✅ | `0.20 → 0.75` в secondary background |

---

## Отклонения от ТЗ

нет отклонений.

---

## Открытые вопросы для Claude

нет.

---

## Что НЕ сделано (если статус ⚠️ или ❌)

—

---

## Подсказки для ревью

- Проверить одну строку `background` в `btnStyle`: обе ветки теперь используют `rgba(255,255,255,0.75)`.
