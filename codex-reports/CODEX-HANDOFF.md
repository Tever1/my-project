# Codex handoff

Обновлено: 2026-08-13.

## Start here

История старого Codex-thread больше не требуется. Перед новой задачей прочитать:

1. [`../AGENTS.md`](../AGENTS.md) — действующие обязательные ограничения;
2. [`../PROJECT_CONTEXT.md`](../PROJECT_CONTEXT.md) — актуальная архитектура,
   продуктовые решения и принятые дизайны;
3. [`../TASKS.md`](../TASKS.md) — текущая очередь;
4. [`../docs/decisions/`](../docs/decisions/) — причины важных решений.

`PROJECT_STATUS.md` — только совместимый указатель. Номерные отчёты и Git
содержат историю, но не нужны для обычного продолжения.

## Task numbering

- Последняя задача: **TASK-439** (долговременный контекст).
- Следующий свободный номер: **TASK-440**.
- Сохранять последовательную нумерацию и отдельный отчёт в `codex-reports/`.

## Protected AGENTS.md

Корневой `AGENTS.md` не был изменён: действующая версия безусловно запрещает
Codex редактировать её и принадлежит Claude. Точная компактная замена с
запрошенными долговременными правилами подготовлена в
[`../docs/AGENTS_UPDATE_PROPOSAL.md`](../docs/AGENTS_UPDATE_PROPOSAL.md).
Её применение владельцем — первая задача в `TASKS.md`.

## Current checkpoint

- Дизайны Mafia «Закрытый клуб», Spy «Оперативный центр» и Alias «Карточная
  мастерская» завершены и приняты.
- Большой продуктовый этап после Git-коммита `0fefa94` ещё не закоммичен;
  сохранять все существующие изменения и не откатывать чужую работу.
- `npx tsc --noEmit` и `git diff --check` проходят.
- `npm run lint` падает на `src/app/alias-design-preview/page.tsx:330`
  (`react-hooks/set-state-in-effect`) и показывает 4 warning legacy renderers
  Мафии. Это внесено в `TASKS.md`.
- Browser QA не запускать без явного запроса пользователя.
- Не коммитить и не пушить без явной команды.

Подробный отчёт перехода: `439-long-term-project-context.md`.
