# TASK-NNN: <короткое название>

> **Метаданные** (заполняет Claude перед стартом)
> - **Дата создания:** YYYY-MM-DD
> - **Сложность:** simple | complex
> - **Запуск:** auto by Claude | manual by user (Codex Desktop)
> - **Ожидаемое время Codex:** ~N минут
> - **Зависит от тасков:** —

---

## Цель

<Одно-два предложения: что должно получиться в результате. Никакого "как",
только "что".>

---

## Контекст

<Почему этот таск нужен, что предшествовало, ссылки на коммиты/багрепорты/
issue. 2-5 предложений.>

---

## Файлы к изменению (whitelist)

Codex имеет право редактировать **только** эти файлы. Любое отклонение —
сначала спросить (через раздел «Открытые вопросы» в отчёте).

- `path/to/file1.ts` — <что меняем>
- `path/to/file2.tsx` — <что меняем>

### НЕ ТРОГАТЬ

- `path/to/legacy.ts` — <причина>
- никакие файлы вне whitelist
- `CLAUDE.md`, `AGENTS.md` — обновляет только Claude

---

## Шаги реализации

1. <конкретное действие>
2. <конкретное действие>
3. <конкретное действие>

> Если по ходу выясняется что нужен дополнительный шаг или другой файл —
> остановиться, написать в отчёт, вернуть управление Claude.

---

## Acceptance criteria

- [ ] `npm run lint` без новых ошибок
- [ ] `npm run build` успешен (для production-кода)
- [ ] <функциональная проверка 1>
- [ ] <функциональная проверка 2>

---

## Ограничения и подводные камни

- **i18n:** все user-facing строки должны быть в паре `{ ru, en }`. Исключение —
  preview/dev-страницы (`/design-tokens`, `/lobby-preview` — только русский).
- **Host-authoritative:** игровая логика только в `server.mts`. Клиент не
  принимает решений о состоянии игры.
- **Alias classic mode НЕПРИКОСНОВЕНЕН** — если правишь Alias, проверь что
  не ломаешь classic.
- **Комментарии в коде** — английский (техническая конвенция).

---

## Контрольные точки для самопроверки Codex

Перед тем как считать таск выполненным, Codex должен:

1. Прочитать diff (`git diff --stat` + `git diff`).
2. Убедиться что не вышел за whitelist файлов.
3. Запустить `npm run lint` и `npm run build`.
4. Заполнить отчёт `.codex/reports/NNN-<name>.md` по шаблону `_TEMPLATE.md`.
5. **Не коммитить.** Коммит делает Claude после ревью (или пользователь).

---

## Открытые вопросы для Codex

<Если Claude знает о неоднозначностях, перечислить здесь, чтобы Codex не
гадал. Например:>

- Нужно ли логировать N? — **нет, не нужно**.
- Использовать Framer Motion или CSS-only анимацию? — **CSS-only, чтобы
  не раздувать bundle**.
